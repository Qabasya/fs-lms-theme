# FS LMS Theme — план разработки

Блочная (FSE) WordPress-тема с нуля: собственная дизайн-система, кастомные
Gutenberg-блоки и библиотека паттернов, из которых страницы собираются
вручную в редакторе (как в WoodMart, но на нативных блоках, без Elementor).

## Принципы архитектуры

- **Тема = представление.** Вся бизнес-логика (курсы, преподаватели, группы,
  заявки, авторизация) остаётся в плагине `fs-lms` — его контроллеры
  (`ApplyPageController`, `AuthPageController`, `SubjectLandingController`)
  сами рендерят свои страницы. Тема на них не покушается: CTA-кнопки на
  маркетинговых страницах ведут на URL этих страниц, а не дублируют форму.
- **Контент паттернов на старте — статичный.** Редактор вручную вписывает
  текст/картинки в блоках (карточка курса, преподавателя и т.д.). Без
  `WP_Query`/REST к CPT плагина — это осознанно отложено, чтобы не размывать
  границу тема/плагин на первом проходе. Если понадобится "живая" секция
  (например, реальный список курсов на главной), это отдельная, явно
  обсуждённая задача.
- **Дизайн-токены — источник один: `fs-lms` plugin `src/scss/frontend/_variables.scss`
  + `src/scss/shared/_tokens.scss`.** `theme.json` уже синхронизирован с ними
  (accent `#3b5bdb`, bg `#f3f4f7`, radius/shadow — см. сверку ниже), держим
  это соответствие как инвариант при любых правках токенов.
- **Сборка — gulp+webpack, как в плагине** (`fs-lms/gulpfile.js`), не
  `@wordpress/scripts`. Та же схема путей `src/ → assets/` (билд
  гитигнорится), тот же postcss/autoprefixer/cssnano, webpack добавляем и для
  JSX кастомных блоков.
- **Кастомные блоки — там, где нужна гибкость** (карточки, hero, FAQ, CTA);
  чисто композиционные секции (сетки, отступы, шапка/футер) — паттерны из
  core-блоков + кастомных карточек.
- Источник дизайна карточек и секций главной — `design-mockup/project/Главная
  - мокап.dc.html` (Claude Design хэндофф). Это dev-референс, не часть
  рантайма темы.

## Текущее состояние (аудит, не переделывать с нуля)

Сделано и рабочее — используем как основу:
- `theme.json` — токены синхронизированы с плагином (цвета/шрифты/spacing/radius/shadow). ✅
- `functions.php` — базовые `add_theme_support`, регистрация категорий паттернов, подключение Ubuntu/JetBrains Mono тем же способом, что и `BundleLoader::enqueueUiFont()`. ✅
- `templates/page.html`, `index.html`, `404.html` — стандартная FSE-разметка, ок. ✅
- `parts/header.html`, `parts/footer.html` — рабочий, но упрощённый вариант (не по мокапу). ⚠️ заменить на паттерны ниже.
- `patterns/header-nav.php`, `footer-columns.php`, `hero-split.php` — черновики, частично повторяют мокап. ⚠️ доработать по мокапу и переписать `hero-split` на кастомный блок Hero.

Требует переделки:
- `templates/front-page.html` — сейчас это **сырой HTML мокапа в одном `wp:html` блоке** (инлайновые стили, ничего не редактируется в Gutenberg). Это заглушка, а не блочная сборка — разобрать на паттерны/блоки ниже и пересобрать страницу из них.
- Сборки CSS/JS нет вообще (`package.json`, `gulpfile.js` в теме отсутствуют).
- Кастомных блоков нет.
- Из мокапа разобрана только шапка/hero — features, alumni-strip, courses grid, intensive-split, stats, blog-grid, contact form, footer ещё не превращены в паттерны/блоки.

---

## Фаза 0 — Подготовка репозитория ✅

- [x] Решено: `design-mockup/` коммитится как dev-референс (уже отслеживается в git с Фазы 1, 18 файлов) — не гитигнорится и не исключается из архива. Выбор зафиксирован комментарием в `.gitignore` темы.
- [x] `.gitignore` темы создан ещё в Фазе 1: `/assets/`, `/maps/`, `node_modules/`, `*.log`, IDE/OS-мусор.
- [x] Перепроверено `fs-lms/src/scss/frontend/_variables.scss` + `shared/_tokens.scss`: `$hl-*` (цвета подсветки кода) в `theme.json` не переносились — не пропажа, а следствие решения Фазы 3 упростить hero-код-блок мокапа до `wp:image`-плейсхолдера (`patterns/hero-split.php`), так что подсветка синтаксиса в теме не используется нигде. `$tag-palette` — 5 из 6 ступеней (`accent`/`wait`/`ok`/`info`/`err` + их soft-варианты) уже есть в `theme.json` из коробки; 6-я («muted», `#f0f1f4`) и не нужна: карточные бейджи (Фаза 4, `BADGE_COLORS` в `src/blocks/shared/colors.js`) — отдельный от тегов-предметов набор (включает `violet`/`practice`, которых нет в `$tag-palette`), одинаковый набор им не требуется.

## Фаза 1 — Инструменты сборки (gulp + webpack, по образцу плагина) ✅

- [x] `package.json` темы: `gulp`, `gulp-sass`+`sass`, `gulp-postcss`+`autoprefixer`+`cssnano`, `gulp-sourcemaps`, `gulp-rename`, `gulp-plumber`+`gulp-notify`, `webpack`+`webpack-stream`+`babel-loader`+`vinyl-named` — как у плагина.
- [x] Добавить то, чего у плагина нет, но нужно для кастомных блоков: `@babel/preset-react` как devDependency + `glob` для динамического поиска блоков. Пробовали держать `@wordpress/blocks`/`block-editor`/`components`/`element`/`i18n` в devDependencies ради автокомплита в IDE (в рантайме это глобалы `wp.*`, не бандлятся) — убраны позже: их собственное дерево зависимостей конфликтовало по версии `stylelint` и ломало `npm ci` в CI под Node 20 (см. коммит «Убрать неиспользуемые @wordpress/* devDependencies»). Для сборки они не нужны — webpack `externals` не резолвит пакет из `node_modules`, только подставляет глобал.
- [x] `gulpfile.js`: адаптированы пути под тему —
  - `src/scss/theme.scss` → `assets/css/theme.min.css` (фронт)
  - `src/scss/editor.scss` → `assets/css/editor.min.css` (стили внутри редактора)
  - `src/js/theme.js` → `assets/js/theme.min.js` (фронт-интерактив: мобильное меню, FAQ-аккордеон, если не native `<details>`)
  - каждый `src/blocks/<name>/index.js` — отдельная webpack-точка входа (собирается динамически через `glob`, без хардкода) → `assets/js/blocks/<name>.min.js`; `style.scss`/`editor.scss` → `assets/css/blocks/<name>.min.css` / `<name>-editor.min.css`.
- [x] Webpack config для блоков: `externals` — `@wordpress/blocks`→`wp.blocks`, `@wordpress/block-editor`→`wp.blockEditor`, `@wordpress/components`→`wp.components`, `@wordpress/element`→`wp.element`, `@wordpress/i18n`→`wp.i18n`, `@wordpress/data`→`wp.data`, `react`→`React`, `react-dom`→`ReactDOM`. Проверено смоук-тестом: бандл блока ~1KB, `wp.blocks`/`wp.blockEditor` не забандлены.
- [x] Babel: `@babel/preset-react` (`runtime: 'automatic'`) — только в конфиге для блоков, обычный `theme.js` его не тянет.
- [x] `npm run build` / `npm run watch` таски в `package.json`. `npm run build` и `npm run watch` проверены живьём — оба стартуют и собирают без ошибок.
- [x] Структура папок: `src/scss/`, `src/js/`, `src/blocks/` (+ `README.md` с конвенцией), `assets/` (пустая, гитигнорится — добавлен `.gitignore` темы: `node_modules/`, `/assets/`, `/maps/`, IDE/OS-мусор).

## Фаза 2 — Дизайн-система ✅

- [x] Дополнен `theme.json`: добавлены `ok-soft`/`err-soft`/`wait-soft`/`info-soft` (точные hex из `frontend/_variables.scss` плагина — `$color-*-soft`) плюс `violet`/`violet-soft` и `practice`/`practice-soft` (из `shared/_tokens.scss` `$step-type-palette` — цвета иконок «видео»/«практика» на мокапе главной, у плагина не форвардятся во frontend-переменные, но остаются тем же единственным источником токенов). Проверено — все фоновые плашки иконок/бейджей мокапа теперь покрыты палитрой.
- [x] `src/scss/theme.scss` — box-sizing reset, `img{max-width:100%}`, общий стиль блока `.is-style-card` (card look: белая поверхность/рамка/тень/hover-подъём, используют `--wp--custom--radius--card` и `--wp--custom--shadow--*`, уже заданные в `theme.json`) и утилита `.fs-placeholder-tile` (диагональная сетка-заглушка под картинку — тот же приём, что в мокапе, но переиспользуемым классом вместо инлайн-градиентов на каждом блоке).
- [x] `src/scss/editor.scss` — те же правила, заскоуплены под `.editor-styles-wrapper`.
- [x] `inc/Assets.php`: `add_editor_style('assets/css/editor.min.css')` (условно — только если файл собран) + `wp_enqueue_style`/`wp_enqueue_script` для `theme.min.css`/`theme.min.js` с версией через `filemtime()`, тем же способом, что `BundleLoader` плагина.
- [x] Регистрация паттерн-категорий и enqueue-логика вынесены из `functions.php` в `inc/Setup.php` (theme supports + `register_block_style('core/group','card')`), `inc/Patterns.php` (категории), `inc/Assets.php` (шрифты + сборка). `functions.php` — тонкий bootstrap, просто `require` модулей `inc/` по списку.
- [x] Проверено: `php -l` на все новые файлы, `theme.json` валиден как JSON, `npm run build` пересобрал `theme.min.css`/`editor.min.css` с новыми правилами — оба файла содержат `.is-style-card` и `.fs-placeholder-tile`.

## Фаза 3 — Шапка и футер (template parts) ✅

- [x] `patterns/header-nav.php` переписан по мокапу: инфо-полоса (`wp:site-tagline` + телефон/email) + основная навигация. Лого — не хардкод «ШБ»-текста, а `wp:site-logo` (32/38px, скруглённый) + `add_theme_support('custom-logo')` в `inc/Setup.php`: редактор загружает реальный логотип, вместо намертво зашитых в паттерн инициалов. Меню — `wp:navigation` с реальными пунктами (Главная/О нас/Курсы/Учебник/Тренажёр), «Главная» ведёт на `home_url('/')` через PHP-интерполяцию (файлы паттернов исполняются WP как PHP через `ob_start()+include`, это проверено отдельным харнессом — см. ниже), остальные пункты — заглушка `#` до появления реальных страниц. Кнопка «Записаться» и «иконка личного кабинета» из мокапа сознательно не стали статичной подделкой (had-coded badge «0» выглядел бы как настоящий счётчик, а он ничего не считает) — сама кнопка есть, реальный маршрут и, если понадобится, иконка ЛК — в Фазе 7 вместе с остальной wiring-логикой на плагин.
- [x] `patterns/footer-columns.php` доработан: лого+название в первой колонке (тот же `wp:site-logo`, консистентно с шапкой), разделитель и цвет линии — через токен `border-light` вместо хардкод-хекса, copyright — динамический год (`gmdate('Y')`) и `bloginfo('name')` вместо зашитого «2026 Шаг в будущее». Колонки «Разделы/Контакты/Соцсети» уже были близки к мокапу — оставлены как есть.
- [x] `parts/header.html` / `parts/footer.html` — тонкие обёртки: `<header>`/`<footer>` + `<!-- wp:pattern {"slug":"..."} /-->`, без своего layout-констрейна (иначе full-bleed фоны паттерна обрежутся до `contentSize`). Паттерн остаётся источником истины, template part его только подключает.
- [x] Активный пункт меню («Главная») — не миф: `.current-menu-item` реально стилизован (`accent-soft`/`accent-700`, см. `theme.scss`/`editor.scss`), просто пока проставлен вручную через `className` в паттерне (ядро само добавляет этот класс только когда URL пункта совпадает с реальным текущим URL — сейчас у пунктов кроме «Главная» ссылки-заглушки). Полноценное дефолтное `wp_navigation`-меню как отдельная сущность не заводили: `wp:navigation` без `ref` рендерит вложенные `wp:navigation-link` прямо из паттерна — рабочий фолбэк на чистой установке, без сидинга CPT при активации темы.
- [x] Проверено: `php -l` на все изменённые файлы; отдельный харнесс имитирует загрузку паттерна так же, как это делает WP (`ob_start()+include`, с заглушками `home_url()`/`bloginfo()`/`esc_url()`) — PHP-интерполяция подставляется корректно, `<?php` в выводе не остаётся, все `wp:*` HTML-комментарии сбалансированы, JSON во всех атрибутах блоков (10 в шапке, 11 в футере) валиден. `npm run build` пересобрал `theme.min.css`/`editor.min.css` с новыми правилами навигации.

## Фаза 4 — Кастомные блоки (block.json + React) ✅

Каждый блок: `src/blocks/<name>/{block.json,index.js,edit.js,save.js,style.scss}`. Статичный save (без PHP render.php) — контент хранится в разметке поста, редактируется в инспекторе. `editor.scss` по факту не понадобился ни одному блоку (см. ниже почему) — не заводили пустых файлов.

- [x] **`fs-lms/feature-card`** — иконка (фикс. набор из 6 встроенных SVG через `SelectControl`, `src/blocks/shared/icons.js`; загрузка своей — вне рамок Фазы 4, в бэклоге), цвет иконки/подложки — два независимых `PanelColorSettings`, значения берутся живьём из `useSetting('color.palette')` (палитра `theme.json`), не хардкодятся в JS.
- [x] **`fs-lms/course-card`** — картинка (`MediaUpload`, фолбэк `.fs-placeholder-tile`), бейдж (текст + один выбор цвета из `src/blocks/shared/colors.js` — семантическая пара «текст+мягкий фон» вместо двух независимых пикеров, чтобы нельзя было собрать нечитаемое сочетание), заголовок, подпись, кнопка (текст + URL через `TextControl` в инспекторе; `buttonUrl` по умолчанию `#` — реальный маршрут страницы курса/заявки плагина откладывается на Фазу 7, в инспекторе есть `help`-подсказка об этом).
- [x] **`fs-lms/teacher-card`** — фото (тот же `MediaUpload`, круглая рамка), имя, роль/предмет, короткое био, необязательная ссылка на профиль (`TextControl`, рендерится только если заполнена).
- [x] **`fs-lms/testimonial-card`** — аватар (фото либо инициалы из имени, `src/blocks/shared/text.js`, вычисляются одинаково в edit и save), имя, роль, текст отзыва, рейтинг — `RangeControl` 0–5 в инспекторе, рендер звёзд (`src/blocks/shared/rating.js`) + скрытый для зрячих `.screen-reader-text` с числом (утилитный класс добавлен в `theme.scss` — в теме с нуля его иначе просто нет).
- [x] **`fs-lms/stat-tile`** — крупное число, подпись, описание, цвет числа — `PanelColorSettings` на полной палитре (секция «цифры»).
- [x] **`fs-lms/faq-item`** — `<details>/<summary>`, чистый CSS-аккордеон (стрелка поворачивается через `[open]`), без единой строчки JS. В редакторе — не `<details>` (сворачивание неудобно редактировать), а всегда развёрнутый `<div>` + `ToggleControl` «Открыт по умолчанию на фронте».
- [x] **`fs-lms/cta-banner`** — заголовок, текст, основная + необязательная вторая кнопка, вариант фона solid/soft через `__experimentalToggleGroupControl`.
- [x] Каждый `block.json`: `apiVersion: 3`, `textdomain: fs-lms-theme`, категория `fs-lms-cards` (зарегистрирована через `block_categories_all` в `inc/Blocks.php`), `supports: {"html": false}` — без лишних `align`/`spacing`, которые эти блоки не используют.
- [x] `inc/Blocks.php`: цикл по `glob('src/blocks/*/block.json')` — единственный источник правды по списку блоков, никакого хардкод-массива. Для каждого блока: если собранные `assets/js/blocks/<name>.min.js` / `assets/css/blocks/<name>.min.css` существуют — регистрирует их под хендлами `fs-lms-<name>-editor-script`/`fs-lms-<name>-style` (версия — `filemtime()`), затем `register_block_type($dir)` подхватывает эти хендлы по именам, указанным в самом `block.json`.
- [x] Общие модули `src/blocks/shared/`: `icons.js` (набор иконок feature-card), `colors.js` (семантические пары цвет/подложка для бейджей), `text.js` (инициалы из имени), `rating.js` (рендер звёзд), `ImageControl.js` (переиспользуемый пикер картинки для course-card/teacher-card — не годится для testimonial-card, там аватар с инициалами внутри круга собран отдельно, см. `testimonial-card/edit.js`).
- [x] Babel для блоков переведён на classic JSX pragma (`createElement`/`Fragment` из `@wordpress/element`) вместо `runtime: 'automatic'` — автоматический рантайм тянет `react/jsx-runtime`, которого нет среди `externals`, и ломает сборку; classic pragma работает с уже настроенными `externals` без изменений (`gulpfile.js`).
- [x] Проверено: `php -l` на `inc/Blocks.php`/`functions.php`; все 7 `block.json` — валидный JSON; `npm run build` собрал все 7 блоков (JS 1.7–4.1 KiB на блок, React/`wp.*` не забандлены — проверено грепом); имена `editorScript`/`style` в каждом `block.json` сверены скриптом с хендлами, которые реально регистрирует `inc/Blocks.php` — совпадают у всех семи. Полный прогон через живой `register_block_type()` в загруженном WordPress не делался — в этом окружении нет поднятой БД/MySQL; статическая проверка (JSON, сборка, соответствие хендлов) — предел того, что можно верифицировать без живого стенда.

## Фаза 5 — Паттерны (композиция секций из блоков) ✅

Все — в `patterns/`, категория `fs-lms-sections`, с `Keywords` для поиска в инсёртере. Источник вёрстки/контента — соответствующий блок в `design-mockup/project/Главная - мокап.dc.html`.

- [x] `hero-split.php` — оставлен композицией core-блоков (columns + heading + buttons + image), кастомный блок не понадобился: hero почти весь текстовый, без повторяющихся элементов, которые оправдывали бы блок.
- [x] `features-grid.php` — заголовок секции + 4× `fs-lms/feature-card` в сетке 2×2 (секция «Сделаем вместе — Шаг в будущее»).
- [x] `alumni-strip.php` — плашка «Наши выпускники поступают» + 4 логотипа-плейсхолдера вузов (`fs-placeholder-tile` + подпись названием — карусель не по мокапу, там статичная сетка 4 в ряд).
- [x] `courses-grid.php` — заголовок + ссылка «Все услуги» + 3× `fs-lms/course-card`.
- [x] `intensive-split.php` — двухколоночная секция (декоративная сетка миниатюр слева на `fs-placeholder-tile`, список преимуществ + цена/CTA справа).
- [x] `stats-row.php` — 3× `fs-lms/stat-tile` в ряд.
- [x] `blog-grid.php` — заголовок «Как проходят наши занятия» + `core/query`+`core/post-template` (реальные посты блога, не статичные карточки — дата/автор/эксцерпт/«продолжить чтение» через `core/post-date`, `core/post-author-name`, `core/post-excerpt`, `core/read-more`).
- [x] `testimonials.php` — сетка из `fs-lms/testimonial-card` (3 примера), без JS-слайдера (см. backlog). Не привязан к мокапу главной — такой секции там нет, паттерн для общей библиотеки.
- [x] `faq.php` — заголовок + 4× `fs-lms/faq-item`. Не привязан к мокапу главной (там такой секции нет).
- [x] `cta-banner.php` — обёртка секции вокруг `fs-lms/cta-banner`. Не привязан к мокапу главной (там CTA встроен в форму контактов, см. `contact-section.php`).
- [x] `contact-section.php` — левая колонка (заголовок/текст/контакты), правая — статичная витрина полей для вида + кнопка-ссылка на страницу заявки плагина через `fs_lms_theme_url('apply')` (не форма с реальной отправкой — это зона плагина).
- [x] `header-nav.php`, `footer-columns.php` — см. Фазу 3.
- [x] Проверено: `php -l` на все файлы паттернов; отдельный харнесс (аналогичный харнессу Фазы 3) проверил баланс всех `wp:*` HTML-комментариев и валидность JSON во всех атрибутах блоков по всем 12 файлам `patterns/*.php` — без ошибок. `npm run build` пересобрал все ассеты без ошибок.

## Фаза 6 — Главная страница ✅

- [x] Полностью пересобран `templates/front-page.html`: убран `wp:html`-дамп мокапа, вставлены паттерны Фазы 5 по порядку мокапа (`wp:template-part` шапка → `wp:pattern` hero → features → alumni → courses → intensive → stats → blogn → contact → `wp:template-part` футер) через `wp:pattern` блоки внутри `<main>`.
- [x] Живой прогон: поднят `docker compose` из репозитория (`wp_db`+`wp_app`, WordPress + активный плагин `fs-lms` на реальной dev-БД), тема скопирована в `wp-content/themes/fs-lms-theme` и активирована (`wp theme activate`). `wp eval` с `WP_Block_Type_Registry`+`parse_blocks()` по всем `templates/*.html`+`patterns/*.php`+`parts/*.html` подтвердил: каждый использованный блок зарегистрирован (`ALL BLOCKS REGISTERED`) — это тот же путь разбора, что использует редактор при вставке паттернов, значит ни одна секция не даст «invalid block». `curl` на `http://localhost:8080/` — HTTP 200, все ожидаемые секции присутствуют в HTML («Сделаем вместе», «Наши выпускники поступают», «Курсы и направления», «Интенсивная подготовка», «Как проходят наши занятия», «Приготовься сделать Шаг в будущее»), `fs_lms_theme_url('apply')` резолвится в реальный `http://localhost:8080/apply/` плагина. `debug.log` контейнера — без единой ошибки/warning от кода темы. Ручной клик по редактированию/дублированию блоков внутри UI редактора (визуальное взаимодействие мышью) не воспроизводился — за пределами того, что можно сделать headless-проверкой, но структурная валидность (то, что чаще всего и «разваливается») подтверждена.
- [x] Визуальная сверка с мокапом: headless-скриншот Chrome (`chrome --headless --screenshot`) страницы `localhost:8080/` — порядок секций, пропорции, отступы, скругления и цвета (акцентный синий, мягкие подложки бейджей по классам 9/10/11, тени карточек) совпадают с `design-mockup/project/Главная - мокап.dc.html`. Отличия — ожидаемые: лого показывает текст сайта вместо квадрата «ШБ» (логотип не загружен — это делает редактор через `wp:site-logo`, не тема), плейсхолдеры вместо реальных фото курсов/выпускников/блога.
- [x] Примечание: копия темы в `wp-content/themes/fs-lms-theme` на dev-стенде — разовый снимок (`robocopy`, без `node_modules`), не живая синхронизация; при дальнейшей разработке её нужно обновлять повторным копированием или собрать её как git-подмодуль/симлинк, если стенд будет использоваться постоянно (за рамками этой задачи).

## Фаза 7 — Интеграция с плагином (границы, не логика) ✅

- [x] Ссылки на функциональные страницы плагина сведены через хелпер `fs_lms_theme_url( $route )` в `inc/PluginRoutes.php`: если плагин активен — слаг резолвится из его же `Inc\Enums\Wp\PageRoutes` enum (переживёт смену слага на стороне плагина); плагин не даёт под это `apply_filters` (проверено — `PageRoutes::url()` строит `home_url('/'.slug.'/')` напрямую, без хука), поэтому фолбэком тот же `home_url()`-паттерн повторяется локально. Применено во всех паттернах, где раньше была заглушка `#` на «Записаться»/CTA к заявке: `header-nav.php`, `hero-split.php`, `courses-grid.php` (3 карточки), `intensive-split.php`, `cta-banner.php`, `contact-section.php`.
- [x] Дублирование шрифтов устранено: `inc/Assets.php` теперь вешается на `wp_enqueue_scripts` с приоритетом 20 (после плагинового `Inc\Core\Enqueue`) и проверяет `wp_style_is('fs-lms-ubuntu', ...)` — хендл, под которым плагин грузит Ubuntu 400/500/700 через `BundleLoader::enqueueUiFont()` на каждом фронт-запросе. Если хендл уже есть — тема доgружает отдельным лёгким запросом только то, чего у плагина нет (вес 300 + JetBrains Mono), вместо повторного заказа всего семейства Ubuntu.
- [x] Чек-лист пройден: `grep` по всей теме на `wpdb`/`WP_Query`/`get_posts`/`Repository`/`Service::`/`Controller::` не нашёл ни одного обращения — единственная точка соприкосновения с плагином — чтение слага маршрута через `PageRoutes` enum (URL-роутинг, не бизнес-логика/данные).
- [x] Проверено: `php -l` на `inc/PluginRoutes.php`, изменённый `inc/Assets.php`, `functions.php` и все паттерны с новыми вызовами `fs_lms_theme_url()` — без ошибок.

## Фаза 8 — QA

- [x] Проверено вживую (см. Фазу 6): `wp eval` с `WP_Block_Patterns_Registry`+`WP_Block_Type_Registry` на поднятом стенде подтвердил — все 13 паттернов зарегистрированы под своими `fs-lms-theme/*` слагами с правильными категориями (`fs-lms-sections`/`fs-lms-layout`) и ключевыми словами (три паттерна Фазы 3 их не имели — дополнены при этом проходе), все 7 кастомных блоков `fs-lms/*` зарегистрированы. Это тот же реестр, который питает превью/поиск в инсёртере Gutenberg — сам клик по UI инсёртера (визуальное превью карточки) headless-проверкой не воспроизводился.
- [x] Адаптивность: mobile-меню (`overlayMenu:"mobile"`, паттерн `header-nav.php`, Фаза 3) — стандартное поведение `core/navigation`, отдельного CSS не требует. Сетки карточек: добавлен `@media (max-width: 960px)` в `src/scss/theme.scss`, складывающий `.wp-block-columns` в одну колонку тем же брейкпоинтом, что и `$bp-tablet` плагина (`fs-lms/src/scss/shared/_tokens.scss`), а не дефолтным WP-брейкпоинтом core/columns (782px).
- [x] Доступность: `<img>` в паттернах и в `fs-lms/course-card`/`teacher-card`/`testimonial-card` (Фаза 4) везде несёт `alt` (пустой `imageAlt` по умолчанию, но атрибут есть); декоративные заглушки без реальных фото — это `<div class="fs-placeholder-tile">`, а не безымянные `<img>`, так что требование alt на них не применимо. Контраст текста на цветных подложках — тот же приём «текст+мягкий фон» из палитры theme.json, что уже проверялся в Фазе 4 (`src/blocks/shared/colors.js`). `<details>/<summary>` для FAQ — нативно, без JS (Фаза 4).
- [x] `npm run build:prod` (новый скрипт, `gulp build --production`) — собирает без sourcemaps (проверено: 0 вхождений `sourceMappingURL` в собранных CSS/JS); обычный `npm run build`/`npm run watch` по-прежнему пишет sourcemaps для разработки. `git status` подтверждает, что `assets/` не отслеживается (см. `.gitignore` темы, Фаза 1).

## Фаза 9 — Документация

- [x] `README.md` темы: как собрать (`npm install && npm run build`/`build:prod`), структура папок, как добавить новый блок/паттерн, где искать токены.

## Фаза 10 — Редакторский workflow: страницы, «О нас», магазин WooCommerce

Цель: любую новую публичную страницу можно собрать в Site Editor из
существующих паттернов без участия разработчика (текст/картинки/порядок
блоков редактируются вручную), плюс две конкретные страницы — «О нас» с
аккордеоном и «Магазин» с карточками товаров WooCommerce.

### 10.1 — Кастомизация главной (без кода)

- [x] Уже возможно: `templates/front-page.html` собран из паттернов
  (Фаза 6), весь контент (текст/картинки/порядок секций) редактируется в
  Site Editor. Отдельного кода не требует — редактор просто открывает
  «Главная» и меняет блоки.
- [ ] Явно задокументировать в README, какие блоки на главной можно
  безопасно переставлять местами/удалять, а какие завязаны на
  `fs_lms_theme_url()` и трогать их URL-логику вручную не нужно (см. раздел
  README «Как собрать новую страницу»).

### 10.2 — Страница «О нас» → `/about/`, обязательная юридическая страница (аккордеон)

Уточнено по `refs/Об образовательной организации - ЕГЭ по информатике.html`:
это не маркетинговый рассказ о школе, а обязательная по закону страница
лицензированной образовательной организации (12 разделов: «Структура и
органы управления», «Сведения о реализации образовательных программ»,
«Образование», «Руководство», «Педагогический состав», «Материально-
техническое обеспечение», «Платные образовательные услуги», «Финансово-
хозяйственная деятельность», «Вакантные места для приёма», «Стипендии и
меры поддержки», «Международное сотрудничество», «Организация питания»).
Реальный контент уже есть в сохранённом HTML — переносится, а не
придумывается заново. Там же — 3 ссылки на существующие PDF в медиатеке
сайта (программа ЕГЭ, положение об оказании платных услуг, шаблон
договора) — при сборке страницы либо переиспользовать те же файлы (URL
`wp-content/uploads/...` не зависит от темы), либо перезалить в медиатеку.

- [x] **Техническое ограничение**: атрибут `answer` блока `fs-lms/faq-item`
  (`src/blocks/faq-item/block.json:13`) — одиночный `RichText` в один `<p>`,
  без множественных абзацев/списков/ссылок на PDF. Контент юридической
  страницы (несколько абзацев на раздел, списки преподавателей, ссылки на
  документы) в эту схему не влезает. Решение: **не переиспользовать
  `fs-lms/faq-item`** для этой страницы — аккордеон собран как статичная
  разметка `<details>/<summary>` внутри одного `wp:html`-блока паттерна
  (текст внутри — не Gutenberg-блоки: контент юридический, редактируется
  редко и напрямую в коде, а не через инспектор).
- [x] `patterns/about-accordion.php` — 12 секций `<details>`, весь текст
  перенесён дословно из `refs/Об образовательной организации - ЕГЭ по
  информатике.html`. Стили — `.fs-legal-accordion` в `src/scss/theme.scss`
  (тот же визуальный язык, что у `.fs-faq-item`: стрелка, разделитель,
  приглушённый цвет ответа).
- [x] Ссылки на 3 PDF внутри паттерна указывают на текущие пути медиатеки
  (`future-step.ru/wp-content/uploads/...`) как есть — работают, если это
  та же БД/медиатека; на чистой установке файлы перезалить и поправить пути.
- [x] Пункт «О нас» в `patterns/header-nav.php:56` ведёт на
  `home_url('/about/')` вместо заглушки `#`.
- [x] **Ручной шаг за пределами кода темы**: страница `/about/` создана
  (`wp_insert_post()` через `wp eval`, ID 19627, `post_name=about`,
  шаблон `page`, оба паттерна — `about-header`+`about-accordion` — в
  `post_content`). **Повторно** — уже делалось так же в Фазе 15, но
  страница пропала из БД к 2026-09-03 (пользователь получил 404 на
  `/about/`, `wp post list` подтвердил отсутствие страницы) — судя по
  всему, dev-стенд (Docker) пересоздавался/сбрасывался между сессиями.
  Держать в уме: на этом стенде БД не персистентна между сессиями,
  ручные `wp_insert_post()`-шаги могут понадобиться повторно.
- [x] `npm run build` — прошёл чисто, `.fs-legal-accordion` подтверждён в
  собранном `theme.min.css`.
- [ ] Живой прогон на dev-стенде (аналогично Фазе 6, после ручного шага
  выше): `wp eval` подтверждает регистрацию паттерна, `curl`/скриншот
  `/about/` показывает все 12 разделов и рабочий аккордеон без JS.

### 10.3 — Подключение WooCommerce к теме (база)

Реальные данные каталога уже видны в `refs/Абонементы - ЕГЭ по информатике
Калининград.html`: 4 категории товаров — «ЕГЭ по информатике» (`/kege/`, 3
товара), «ОГЭ по информатике» (`/koge/`, 1), «Python 10 класс» (`/python-10/`,
1), «Робототехника 4-6 класс» (`/robo/`, 1); товары — «11 класс Абонемент на
месяц/год/персональный», «10 класс...», «9 класс...», «5-6 классы...». Если
эти категории/товары ещё есть в проде WooCommerce — переносить не нужно,
только сверить slug'и; если стенд чистый — завести те же для реалистичной
проверки вёрстки.

- [x] `add_theme_support( 'woocommerce' )` в `inc/Setup.php`.
- [x] `add_filter( 'loop_shop_columns', fn() => 3 )` — сетка 3 карточки в
  ряд, новый модуль `inc/WooCommerce.php`, добавлен в список `require` в
  `functions.php`. `php -l` и `npm run build` прошли чисто.
- [ ] **Ручной шаг**: активировать плагин WooCommerce на dev-стенде,
  завести тестовые товары по образцу реальных категорий/названий выше —
  нужно для живой проверки каталога в 10.4/10.5 (без активного плагина
  `add_theme_support('woocommerce')`/хуки в `inc/WooCommerce.php` не на
  чем проверить).

### 10.4 — Страница магазина: сайдбар и тулбар ✅ (код; живая проверка — см. ниже)

- [x] `register_sidebar()` под зону `shop-sidebar` (`inc/Setup.php`,
  `widgets_init`) — `before_widget`/`after_widget` под класс `.fs-widget`.
- [x] `sidebar-shop.php` — если сайдбар не настроен вручную в Виджетах,
  дефолт из кода: `the_widget('WC_Widget_Product_Categories')` +
  `the_widget('WC_Widget_Price_Filter')`.
- [x] Хуки `woocommerce_before_main_content`/`woocommerce_after_main_content`
  в `inc/WooCommerce.php` — обёртка `.fs-shop-layout` (сайдбар + контент),
  внутри вызывает `get_sidebar('shop')`. Перед реализацией сверено по
  исходникам в `refs/woocommerce/src/Blocks/BlockTypes/ClassicTemplate.php`:
  даже в блочной FSE-теме дефолтный `archive-product.html` рендерит каталог
  через блок `woocommerce/legacy-template`, который вызывает те же
  классические хуки, что обычный `archive-product.php` — переопределять
  блочный шаблон не потребовалось.
- [x] `remove_action` рейтинга на карточке (`woocommerce_template_loop_rating`)
  — в референсе WoodMart звёзд нет, у курсов-абонементов отзывов не ведут.
- [x] Стили `.fs-shop-layout`, `.fs-shop-sidebar`, `.woocommerce-result-count`,
  `.woocommerce-ordering`, `.fs-widget` (категории, ценовой слайдер) —
  новый партиал `src/scss/_woocommerce.scss`, `@import` в `theme.scss`, под
  токены `theme.json`; сложение в 1 колонку на 960px (тот же брейкпоинт,
  что у остальных сеток). `php -l` и `npm run build` — чисто, классы
  подтверждены в собранном `theme.min.css`.
- [ ] Живая проверка на dev-стенде (нужен активный WooCommerce + товары из
  10.3, ещё не заведены) — сетка/сайдбар/тулбар визуально, до 10.6 не
  прогонялось.

### 10.5 — Карточка товара и quick view ✅ (код; живая проверка — см. 10.6)

(`remove_action` рейтинга уже сделан в 10.4 — реализован вместе с остальными
хуками `inc/WooCommerce.php`, отдельно выносить не стал.)

- [x] Хук `woocommerce_after_shop_loop_item_title` (приоритет 6) — первая
  категория товара под заголовком карточки, ссылкой на `get_term_link()`.
- [x] Кнопка quick view — **не** на `woocommerce_before_shop_loop_item_title`,
  как планировалось изначально: этот хук рендерится внутри `<a>`, которую
  WooCommerce открывает на `woocommerce_before_shop_loop_item` (оборачивает
  миниатюру+заголовок+цену) — кнопка внутри `<a>` невалидна как вложенный
  интерактивный элемент. Вместо этого — хук на том же
  `woocommerce_before_shop_loop_item`, но раньше (приоритет 5, до открытия
  `<a>` на приоритете 10): кнопка выходит перед ссылкой в разметке, поверх
  миниатюры её кладёт `position: absolute` от `li.product` (`position: relative`
  в `_woocommerce.scss`).
- [x] `wp_ajax_fs_quick_view` / `wp_ajax_nopriv_fs_quick_view`
  (`inc/WooCommerce.php`) — фото/заголовок/цена/краткое описание/кнопка «В
  корзину» (переиспользует `woocommerce_template_loop_add_to_cart()` — то
  же самое AJAX-добавление, что и в сетке, работает и из модалки без
  дублирования). `check_ajax_referer` на nonce `fs-quick-view`.
- [x] `ajaxUrl`/nonce переданы фронту через `wp_localize_script()` в
  `inc/Assets.php` (объект `fsLmsTheme`) — `admin_url()` на фронте
  недоступен без явной передачи.
- [x] `src/js/quick-view.js` (импорт в `src/js/theme.js`) — делегированный
  клик по `.fs-quick-view` → `fetch` → вставка HTML в модалку; закрытие по
  фону/крестику/Esc. Модуль строит модалку в JS (не в разметке карточек) —
  один DOM-узел на страницу, а не по одному на товар.
- [x] `ul.products`/`li.product` — визуальный стиль в языке `.is-style-card`
  (белая поверхность/рамка/радиус/тень/hover-lift; сам класс не
  переиспользован — WooCommerce не добавляет его на `li.product`,
  задублировать свойства оказалось проще, чем городить общий миксин ради
  одного места использования), сетка 3 колонки → 1 на 960px, в
  `_woocommerce.scss`.
- [x] AJAX-добавление в корзину — код не писали, штатное поведение
  WooCommerce при `add_theme_support('woocommerce')` +
  `woocommerce_template_loop_add_to_cart()`; подтверждается в 10.6 живым
  прогоном (сетевая вкладка), не статически.
- [x] `php -l` (`inc/WooCommerce.php`, `inc/Assets.php`) и `npm run build`
  — чисто; все новые CSS/JS-артефакты (`fs-quick-view*`, `fs_quick_view`,
  `fsLmsTheme`) подтверждены в собранных `theme.min.css`/`theme.min.js`.

### 10.6 — Адаптивность и QA страницы магазина

- [x] `@media (max-width: 960px)` — сайдбар складывается над сеткой в 1
  колонку (тот же брейкпоинт, что у сеток карточек, `theme.scss:104`).
- [x] `php -l` на все новые/изменённые PHP-файлы; `npm run build` без ошибок.
- [ ] Живой прогон на dev-стенде: сетка/сайдбар/тулбар/quick view/AJAX-корзина
  — по чек-листу by (см. план `sequential-nibbling-cat.md`, если ещё не
  реализовано — актуальный источник шагов проверки).
- [ ] Пункт меню на страницу магазина (заглушка `#` в `header-nav.php`,
  аналогично п. 10.2) заменить на реальный URL.

**Явно вне рамок Фазы 10** (отдельная задача при необходимости): редизайн
страницы отдельного товара, офканвас-сайдбар на мобильных, иконка
корзины/счётчика в шапке, wishlist/compare, зум-галерея/photoswipe.

---

## Фаза 11 — Редизайн главной по макету v2 (визуал/вёрстка, без ACF/CPT)

Источники: `refs/Главная v2 - мокап.dc.html` (точный референс — HTML с
инлайн-стилями, каждый px/цвет читается напрямую) и
`refs/Спека главной для WP.md` (список расхождений с текущей сборкой).
Раздел 8 спеки просит ACF-поля/CPT/форму с бэкендом — это новый плагин и
пересмотр решения Фазы 0 («контент статичный»), в этот проход **не входит**
(подтверждено пользователем отдельно) — только визуал в уже принятой
статичной архитектуре паттернов. Текст на странице не меняется.

### 11.1 — Токены: типографика и корневые отступы

- [x] `theme.json` `settings.typography.fontSizes` — пересобрать шкалу под
  макет: `xxs` 12px, `xs` 13px, `sm` 14px, `base` 15px, `md` 16px, `lead`
  18px, `lg` 19px, `xl` 22px, `xxl` 34px (H2), `xxxl` 52px (H1). Слаги не
  трогать (переименование под баг `_wp_to_kebab_case` уже сделано).
  Разовые размеры (чип 11px, число-статистика 40px) — инлайн
  `style.typography.fontSize` в конкретном блоке, не новый токен.
- [x] `theme.json` `styles.spacing.padding` (корневой боковой отступ) —
  `1rem` → `3.75rem` (60px, desktop).
- [x] `theme.scss` — адаптивные брейкпоинты корневого отступа: 32px
  (900–1199px), 20px (<600px) через `--wp--style--root--padding-*`.
- [x] Вертикальный ритм между секциями (88px, первая секция — 72px) —
  инлайн `padding-top` в каждом паттерне-секции (`5.5rem`/`4.5rem`).

### 11.2 — Компоненты в `theme.scss`

- [x] Кнопки: высота 38px (шапка/карточки) / 44–46px (главные CTA).
- [x] `:focus-visible` на кнопках/полях формы — `outline: 2px solid accent;
  outline-offset: 2px` (доступность, отсутствует в теме сейчас).
- [x] Чип на карточке курса — отдельный класс заводить не понадобилось:
  `fs-lms/course-card`'s `&__badge` (`src/blocks/course-card/style.scss`)
  уже pill/`accent-tint`/uppercase, ровно то, что просит спека.
- [x] `.fs-form-field` — поле формы CTA (label 13px/500 + инпут 42px, фон
  `surface-2`, фокус — рамка accent + мягкое `box-shadow`-кольцо).

### 11.3 — Паттерны секций главной

- [x] `hero-split.php` — правая колонка с карточкой кода `main.py`
  (разметка с подсветкой синтаксиса уже есть в мокапе, инлайн-стили);
  сетка `minmax(0,1fr) 520px`; полоса из 3 фактов через `border-top`.
- [x] `alumni-strip.php` — убрать белую карточку-обёртку, ряд логотипов
  прямо на сером фоне, подпись `12px/uppercase/muted`.
- [x] `features-grid.php` — маркеры `»` → обычные абзацы без маркера;
  сетка карточек `display:grid` + `align-items:stretch`, без фиксированных
  высот и обрезки `overflow`.
- [x] `courses-grid.php` — обложка `aspect-ratio:16/10` + `object-fit:cover`;
  нижняя строка карточки — цена слева / кнопка справа, `margin-top:auto`.
  Атрибут `price` в блоке `fs-lms/course-card`
  (`src/blocks/course-card/{block.json,edit.js,save.js}`) — добавить, не
  ломая существующие атрибуты.
- [x] `intensive-split.php` — фото `aspect-ratio:4/3`; чек-лист — иконка
  галочки в кружке `accent-tint` вместо `»`; плашка «2 часа / 800 ₽» —
  одна карточка с вертикальным разделителем.
- [x] `stats-row.php` / блок `fs-lms/stat-tile` — число 40px/700/accent,
  подпись 16px/700, пояснение 14px/300/muted.
- [x] `blog-grid.php` — не рендерить секцию, если опубликованных постов
  меньше 3 (`wp_count_posts()->publish >= 3`, паттерн уже исполняется как
  PHP — оборачиваем существующую разметку в `<?php if (...) : ?>`).
- [x] `contact-section.php` — реальные поля формы (имя родителя, телефон,
  класс/направление `<select>`) визуально по макету, без бэкенда —
  кнопка по-прежнему ведёт на `fs_lms_theme_url('apply')`. Контакты слева
  — с иконками телефон/почта/пин вместо текста с `»`.
- [x] `header-nav.php` / `footer-columns.php` — точечная сверка px-высот
  (топбар 36px, шапка 68px) и `aspect-ratio:16/9` у карты футера (сейчас
  фиксированная `height:240px`).

### 11.4 — Иконки

- [x] Инлайн SVG (обводка 1.6, `stroke:currentColor`) прямо в PHP
  паттернов — по образцу уже сделанного для корзины/quick-view в Фазе 10:
  галочка чек-листа, телефон/почта/пин в контактах. Отдельный JS-модуль
  иконок не нужен — эти иконки не редактируются через инспектор блока.

### 11.5 — Проверка

- [x] `php -l` на все изменённые `patterns/*.php`.
- [x] `npm run build` — без ошибок.
- [x] Синхронизация на dev-стенд (`robocopy`) + живой `curl`/headless-Chrome
  скриншот — сверить с мокапом посекционно.
- [x] `wp_get_global_stylesheet()` — новые размеры шрифта реально
  генерируются (тот же приём, что использовался для бага с цифровыми
  слагами).

**Явно вне рамок Фазы 11**: ACF-поля, CPT курса, `home_*` группы полей,
реальный бэкенд формы (раздел 8 спеки) — отдельная задача, требует
решения о новом плагине и пересмотра архитектуры Фазы 0.

**Про «блоки как в React»**: архитектура для этого уже есть — каждая
секция главной является паттерном, редактор двигает/удаляет/дублирует их
в Site Editor как обычные блоки, вставляет новые между существующими.
Карусель постов/другие новые блоки — по образцу `src/blocks/*` (Фаза 4),
не архитектурное изменение.

---

## Фаза 12 — Редизайн главной по макету v4

Источник: Claude Design проект `76381180-848a-43eb-be89-7071cd1c5f9a`, файл
`Главная v4 - сборка.dc.html` (полная сборка всех блоков главной в одном
файле, HTML вытащен и разобран целиком) + сопутствующая
`Спека главной для WP.md` из того же проекта. Это следующая итерация после
Фазы 11 — точечной сверки текущих паттернов с v1/v2-мокапами; здесь макет
меняется полностью и структурно (новый первый экран, две новые карусели,
тёмный футер), не точечная правка px.

**Уже принятые решения (не пересматривать без явного запроса
пользователя):**

1. Токен `accent` в `theme.json` — заменить `#375FA3` → `#3b5bdb` (и
   производные: hover/700 под `#2f44b3`, мягкий тон под `#edf2ff`).
   Используется по всей теме, не только на главной.
2. Логотип — оставить `images/logo.png` (не переходить на инлайн-знак из
   4 цветных полосок, который в макете v4), только подогнать
   размеры/отступы под макет.
3. Карусели («Наши выпускники», «Наши выпускники поступают») — подключить
   **Splide** (`@splidejs/splide`) как новую фронт-зависимость: npm-пакет
   + webpack-бандл в `src/js/theme.js`, инициализация `loop`, стрелки,
   `mask-image` на контейнере краёв (не белые градиентные накладки).
4. Контент — без ACF/CPT, как и весь остальной репозиторий сейчас:
   статические паттерны с хардкодным контентом (по образцу
   `courses-grid.php`). Пункты спеки про ACF/CPT/реальный бэкенд формы —
   явно вне рамок (как и в Фазе 11, см. backlog в конце файла).
5. Кнопка «Записаться» (шапка, карточки курсов, блок «Как устроены
   занятия») — везде якорь на форму на той же странице (`#hero-form` из
   шапки/первого экрана, `#signup` из курсов/блока занятий), как в
   макете v4. Страница заявки плагина (`fs_lms_theme_url('apply')`)
   больше не используется с главной — только через другие страницы
   сайта, если где-то ещё ссылается.
6. Карточка выпускника — новый блок `fs-lms/alumni-card`
   (`src/blocks/alumni-card/`, по образцу `testimonial-card`/`course-card`),
   не расширение `testimonial-card`.
7. Соцсети (YouTube/VK/Telegram) — убрать совсем, ни в шапке, ни в
   футере (сейчас в шапке — удалить оттуда; в v4-футер не добавлять).

### 12.0 — Токены и подготовка

- [x] `theme.json`: `accent`/`accent-600`/`accent-700`/`accent-soft` →
  цвета макета v4 (см. решение 1 выше); проверить контраст текста на
  новых фонах (кнопки, ссылки, активный пункт меню).
- [x] `package.json`/`gulpfile.js`: добавить `@splidejs/splide` в
  зависимости и его CSS в сборку (импорт core-стилей Splide в
  `src/scss/theme.scss` или собственный минимальный слой поверх).

### 12.1 — Шапка (`header-nav.php`)

- [x] Верхняя инфополоса 34px `--fs-gray-0` — сверить точные px/цвета с
  v4 (комментарии в файле сейчас ссылаются на v1/v2-мокап — актуализировать).
- [x] Активный пункт меню «Главная» — фон `accent-soft`, цвет `accent`
  (похожий приём уже есть — проверить точные токены совпадают с v4).
- [x] Кнопка «Записаться» — цвет `accent-2` (#DE5904), якорь `#hero-form`
  (решение 5 выше; расходится с текущим `fs_lms_theme_url('apply')`).
- [x] Соцсети (YouTube/VK/Telegram) — их и не было в шапке с самого начала
  (Фаза 3), убирать было нечего, решение 7 уже выполнено фактом отсутствия.
- [ ] Иконка корзины — оставлена как есть (в v4 её в шапке нет, но она не
  относится к соцсетям) — вопрос «убирать её или нет» не решён, нужен
  ответ пользователя, самостоятельно не убирал.

### 12.2 — Новый первый экран (заменяет `hero-split.php`)

- [x] Левая колонка — кремовая панель `#faf5ee`, левый бордер 3px
  `accent-2`, список из 4 направлений (стрелка + название 21px + класс
  13px оранжевым `accent-2`), разделители `#ece3d6` между строками, у
  последней строки разделителя нет.
- [x] Правая колонка — карточка формы `id="hero-form"` (поля ФИО +
  Телефон в ряд, кнопка «Отправить», сноска про политику
  конфиденциальности) + под ней отдельная плашка из 3 фактов
  (84 / 800 ₽ / до 8) с вертикальными разделителями.
- [x] Один паттерн `hero.php` (две колонки в одном файле, по образцу
  текущего `hero-split.php`) — левая «направления» верстается как простой
  список ссылок прямо в этом паттерне, не через `fs-lms/course-card`
  (другой контент/разметка).
- [x] Порядок направлений: ЕГЭ 11 класс, ОГЭ 9 класс, Python 10 класс,
  Робототехника 5–8 класс (как в макете, не как в текущем
  `courses-grid.php`).

### 12.3 — «Наши выпускники» (новая карусель отзывов)

- [x] Новый блок `fs-lms/alumni-card` (решение 6 выше; по образцу
  `src/blocks/testimonial-card/`, не расширение testimonial-card): фото
  92×92 слева, справа — баллы (12px uppercase, `accent-2`), ФИО 17px/700,
  текст 14px/300. Атрибуты: `imageId`/`imageUrl`/`imageAlt`, `scoreText`
  (строка «98 баллов», не число — под разные форматы у разных
  направлений), `authorName`, `quote`.
- [x] Splide-карусель: 3 видимых слайда десктоп / 2 планшет / 1 мобильный,
  `loop:true`, круглые стрелки 36px по краям (белые, бордер, тень),
  `mask-image: linear-gradient(90deg, transparent, #000 14%, #000 86%, transparent)`
  по краям контейнера — не белая градиентная накладка.

### 12.4 — «Наши выпускники поступают» (логотипы вузов, отдельная секция)

- [x] Серая плашка-обёртка с бордером/радиусом — в Фазе 11 такую обёртку
  специально убрали из `alumni-strip.php` (посчитали, что её не было в
  мокапе), в v4 она возвращается — зафиксировать это как разворот
  прежнего решения, не как повторную ошибку.
- [x] Splide-карусель логотипов: 5 в ряд, автопрокрутка `loop`, логотипы
  ч/б, `mask-image` по краям контейнера как в 12.3.

### 12.5 — Преимущества (`features-grid.php`)

- [x] Структура почти совпадает с текущей (левая колонка 340px текст +
  кнопка, правая — сетка 2×2 карточек). 4 нужные иконки макета —
  `users`/`record`/`document`/`spark` — **уже есть** в
  `src/blocks/shared/icons.js`, переиспользовать, не заводить новые.
- [x] Сверить конкретные тексты 3 вопросов-абзацев и 4 карточек с v4
  (частично отличаются формулировки от текущих).
- [x] Кнопка «О занятиях» — якорь на `#lessons` (блок 12.7), сейчас ведёт
  на `#`.

### 12.6 — Курсы (`courses-grid.php` + `fs-lms/course-card`)

- [x] 4 карточки вместо 3 — добавить недостающую «Робототехника»
  (5–8 класс).
- [x] Порядок: Робототехника, Python 10 класс, ОГЭ 9 класс, ЕГЭ 11 класс.
- [x] Обложка `aspect-ratio:16/10` + `object-fit:cover` — уже сделано в
  Фазе 11, сверить с v4 без изменений по сути.
- [x] Кнопка «Записаться» карточки — якорь `#signup` (блок 12.8, решение 5
  выше), не `fs_lms_theme_url('apply')` напрямую.

### 12.7 — «Как устроены занятия» (по сути `intensive-split.php`, добавить `id="lessons"`)

- [x] Структура почти 1:1 с текущей (фото 4:3 + чек-лист 6 пунктов +
  плашка «2 часа / 800 ₽» с вертикальным разделителем + 2 кнопки) —
  минимальная правка.
- [x] Добавить `id="lessons"` на секцию (якорь из 12.5/12.1).
- [x] Сверить точные формулировки 6 пунктов чек-листа с v4 (1–2 пункта
  отличаются от текущих).
- [x] Кнопка «Записаться» — якорь `#signup` (не `fs_lms_theme_url('apply')`
  напрямую, тот же вопрос, что в 12.6).

### 12.8 — Форма записи (по сути `contact-section.php`, добавить `id="signup"`)

- [x] Синхронизировать набор полей с v4: Имя родителя, Телефон, Класс
  (100px) + Направление (`<select>`, в ряд) — сейчас в
  `contact-section.php` есть «ФИО родителя» + «ФИО ребёнка» + Телефон +
  Класс, но нет поля «Направление» — добавить его, решить, убирать ли
  «ФИО ребёнка» (в v4 такого поля нет).
- [x] Кнопка отправки — на всю ширину карточки, высота 42px.
- [x] Контакты слева — с иконками телефон/почта/пин, кликабельные ссылки
  (уже реализовано похоже в Фазе 11, сверить с v4 без изменений по сути).

### 12.9 — Футер (`footer-columns.php`) — концептуальная смена на тёмную тему

- [x] Тёмная тема `#2b2f36`/`#3a3f47` вместо текущей белой — большое
  визуальное расхождение с остальной (светлой) частью сайта,
  зафиксировать как осознанное решение макета v4, не как баг при ревью.
- [x] Сетка футера (актуализировано 2026-09-02, макет менялся): `grid-template-columns:300px 1fr 300px`
  — обе текстовые колонки (лого+описание+адрес слева, «Информация»
  справа) фиксированной ширины 300px, средняя колонка резиновая (`1fr`)
  с картой `aspect-ratio:16/9`, `max-width:412px`, отцентрованной внутри
  неё (`justify-self:center`). 3 колонки: лого + описание +
  адрес/телефон/почта / карта 16:9 (Яндекс, `iframe`, уже есть похожий
  приём) / «Информация» (реквизиты ИП, оферта, политика
  конфиденциальности). Без соцсетей (решение 7 выше
  — макет по-прежнему рисует YouTube/VK/Telegram в футере, но это
  сознательно убрано и не переносится вслед за макетом v4; тот же футер
  без соцсетей переиспользуется на всех 4 страницах направлений, см.
  Фазу 13, решение 4).

### 12.10 — Проверка

- [x] `php -l` на все изменённые/новые `patterns/*.php` — чисто.
- [x] `npm run build` — Splide собирается без ошибок (`assets/css/vendor/splide-core.min.css`),
  блок `fs-lms/alumni-card` собрался (`assets/js/blocks/alumni-card.min.js`),
  бандл не ломает `wpExternals`. `npx stylelint`/`npx eslint` на новые файлы
  — чисто (кроме одного пред-существующего предупреждения `!important` в
  `theme.scss:691`, не относится к Фазе 12).
- [x] Живой прогон в браузере выполнен (Docker-стенд поднят в этой сессии,
  `npm install` доустановил `@splidejs/splide`, которого не было в
  `node_modules`, — без этого `npm run build` падал на `stylesVendor`).
  `wp eval` подтвердил регистрацию всех паттернов/блоков Фазы 12
  (`fs-lms-theme/hero`, `/alumni-carousel`, `/alumni-strip` и т.д.),
  `curl`/`debug.log` — без ошибок PHP. Headless-Chrome вскрыл и починили
  два реальных бага, которые статические проверки (`php -l`/`npm run
  build`/eslint/stylelint) пропустили:
  1. **Обе Splide-карусели не инициализировались** — `new Splide(el)`
     ищет `.splide__track` только у элемента с классом `splide`
     (внутренний `closest()`-чек в `Elements.find()`), а
     `.fs-carousel-mask` этот класс не нёс → консоль кидала `[splide] A
     track/list element is missing.` на обеих секциях. Добавлен класс
     `splide` в `patterns/alumni-carousel.php` и `alumni-strip.php`.
  2. **Стрелки карусели не позиционировались** — `splide-core.min.css`
     (core-сборка без скина, решение 12.0) не содержит правил для
     `.splide__arrow` вообще (только структурные `.splide__track`/`.list`),
     а наш `.fs-carousel-mask .splide__arrow` в `theme.scss` красил
     стрелки, но не позиционировал (`position:absolute; top:50%` и т.д.
     отсутствовали) — обе стрелки рисовались одна под другой в левом
     верхнем углу. Добавлено позиционирование
     (`position/top/transform/left/right`), направление svg для `--prev`
     — по образцу официального `splide.min.css`.
  Заодно докручены найденные в том же прогоне мелкие адаптивные баги
  (см. следующий пункт).
- [x] Постранично сверено с `Главная v4 - сборка.dc.html` визуально
  (headless-скриншоты 1440px) — секции/порядок/отступы/радиусы/тени
  совпадают. Точная попиксельная сверка теней/паддингов до последнего
  px не проводилась (нет инструмента диффа поверх мокапа в этой сессии) —
  визуальных расхождений не замечено.
- [x] Адаптив по брейкпоинтам из спеки проверен на `≥1200px`/`992–1199px`/
  `768–991px` (headless-Chrome с `--window-size` реально применяет эти
  ширины — проверено). **`<768px` (реальный узкий телефон) проверить не
  удалось**: headless Chrome в этом окружении не опускает окно ниже
  ~500 CSS-px ни через `--window-size`, ни через
  `--force-device-scale-factor` — при запросе 375px он молча рендерит на
  ~500px и лишь обрезает скриншот до 375 физических пикселей, отчего
  контент выглядит обрезанным на картинке, хотя реального переполнения
  нет (`document.documentElement.scrollWidth === clientWidth` подтверждено
  временным диагностическим скриптом на 500px). Это ограничение
  инструмента, не подтверждённый баг — но и не проверенный факт для
  истинных 375px. На проверенных ширинах нашли и исправили 2 реальных
  несовпадения со спекой:
  - `.fs-courses-grid` переключался на 2 колонки только при `≤991px`,
    из-за чего в диапазоне 992–1199px (где спека прямо требует «курсы 2
    карточки в ряд») ещё показывались все 4 — брейкпоинт сдвинут на
    `≤1199px`.
  - `.fs-footer-grid` при `≤991px` схлопывался сразу в 1 колонку — спека
    требует 2 колонки в диапазоне 768–991px (1 колонка только `<768px`).
    Добавлен промежуточный брейкпоинт `≤991px` → `1fr 1fr` + отдельный
    `≤767px` → `1fr`. Первая попытка (`grid-template-columns:1fr 1fr` без
    явного `grid-row`/`grid-column`) не сработала: карта (второй элемент
    в DOM, растянутая на всю ширину через `grid-column:1/-1`) вставала
    между двумя текстовыми колонками и раздвигала их по разным рядам —
    добавлено явное `grid-column`/`grid-row` для обеих `__col` и `__map`.
  Заодно защитно исправлены источники потенциального переполнения на
  узких экранах, не дожидаясь точного подтверждения на `<768px`:
  `.fs-hero-dirs__item` (`flex-wrap:wrap` вместо `nowrap` по умолчанию —
  длинное название направления + класс могли не влезать в строку),
  `.fs-hero-form__row input`/`.fs-form-field input,select` (`min-width:0`
  — grid/flex-элементы по умолчанию не сжимаются у́же контента).
### 12.11 — Аккордеон в стиле Mantine

Не относится к макету v4 напрямую (в `Главная v4 - сборка.dc.html` нет
секции FAQ) — независимая задача по существующему компоненту библиотеки
паттернов, ориентир: https://mantine.dev/core/accordion/ (визуальные
параметры, без React-рантайма — тот же приём, что для кнопок/полей в
«Спека главной для WP.md»).

- [x] Редизайн реализован в `src/blocks/faq-item/style.scss`.
  Mantine-вариант — **`separated`**: каждый вопрос своя рамка+радиус
  панель (`--wp--custom--radius--md`), зазор между панелями `.625rem`
  (`& + &`, без правки `patterns/faq.php` — достаточно смежного селектора,
  контейнер трогать не пришлось).
- [x] Шеврон — SVG, но не как отдельный DOM-узел в разметке (не пришлось
  трогать `save.js`/12 `<summary>` вручную в `about-accordion.php`):
  `::after` с `mask-image: url("data:image/svg+xml,...")` +
  `background-color: currentColor` — тот же визуальный результат (реальный
  SVG-чеврон, не CSS-уголок из бордеров, как было раньше), поворот
  `transform: rotate(180deg)` на `[open]` с `transition`.
- [x] `summary::-webkit-details-marker{display:none}` — было и раньше,
  оставлено. **Плавная анимация высоты `<details>` сознательно не
  реализована**: единственный кросс-браузерный CSS-way без JS —
  экспериментальный `@starting-style`+`allow-discrete` (не во всех
  движках, включая часть актуальных Safari/Firefox на момент реализации),
  а JS-путь (grid `0fr→1fr` на классе, переключаемом по `toggle`-событию)
  идёт вразрез с принципом «FAQ/аккордеон — без JS», явно зафиксированным
  при реализации `fs-lms/faq-item` в Фазе 4 и `about-accordion.php` в
  Фазе 10.2. Раскрытие/закрытие — мгновенное (нативное поведение
  `<details>`), анимируются только шеврон и цвет текста вопроса.
- [x] Цвет текста вопроса при hover — `accent` (сразу, без задержки
  открытия); при `[open]` — тоже `accent`, вес 600 (было 700 у обычного
  вопроса — открытый визуально отличим не только цветом).
- [x] Правки только в `style.scss` — `save.js`/`edit.js` трогать не
  пришлось (см. выше, шеврон через CSS `mask-image`, не через разметку).
- [x] `about-accordion.php` использует свою разметку (не блок
  `fs-lms/faq-item` — обоснованно, см. комментарий в файле и Фазу 10.2:
  ответу нужно несколько абзацев/списки/ссылки на PDF, чего блок не
  поддерживает). Не объединялись в один паттерн — но визуально унифицированы:
  `.fs-legal-accordion` в `theme.scss` получил тот же Mantine-`separated`
  редизайн (панель/радиус/зазор/SVG-шеврон/переходы) 1:1 с
  `.fs-faq-item`, только с точными цветами страницы `/about/`
  (`#f8f9fa`/`#f1f3f5`/`#dee2e6` — уже добавлено здесь же, что закрывает
  и пункт 15.1 «сверить с мокапом» заранее).

---

## Фаза 13 — Страницы направлений (ЕГЭ, ОГЭ, Python, Робототехника)

Источник дизайна — `ЕГЭ информатика - мокап.dc.html` (Claude Design
хэндофф, тот же проект, что и Фаза 12). Один визуальный шаблон
переиспользуется под все 4 направления с разным текстом/цифрами — «пока
везде одинаковый шаблон», по формулировке задачи.

### Локальные решения (зафиксированы в обсуждении 2026-09-02)

1. **Хардкод, не CPT/ACF.** 4 страницы направлений — это статичный
   контент (текст/цифры вручную в паттернах, как договорено для всей
   темы — см. «Принципы архитектуры» в начале файла), НЕ через
   `SubjectRepository`/`SubjectPagesRepository` плагина. Собранные
   паттерны редактор вставляет в 4 уже существующие в плагине корневые
   страницы предметов (`SubjectPageType::Overview`, `/{key}/` — их
   содержимое и так пишет редактор, плагин туда не лезет).
2. **Единственная живая связь с плагином** — 2 ссылки в блоке «Хочешь
   больше?»: «Открыть учебник» → `home_url("/{$subjectKey}/articles/")`,
   «Открыть тренажёр» → `home_url("/{$subjectKey}/trainer/")` (слаги
   стабильны — `SubjectPageType::Articles`/`Trainer`→`path()` в плагине,
   `fs-lms/inc/Enums/Wp/SubjectPageType.php`). Ключи предметов (уточнены
   пользователем 2026-09-02): ЕГЭ — `inf_ege`, ОГЭ — `inf_oge`, Python —
   `python`, Робототехника — `robo`.
3. **Шапка** — переиспользуется `header-nav.php` как есть (без правки
   активного пункта меню под конкретное направление — не усложняем).
4. **Футер** — тот же `footer-columns.php`, что и на главной v4, БЕЗ
   соцсетей (решение 7 Фазы 12), несмотря на то что в макете «ЕГЭ
   информатика» футер показывает YouTube/VK/Telegram — не переносить их.
5. **Форма записи** — версия `contact-section.php` (12.8) БЕЗ поля
   «Направление» (на странице направления оно избыточно — направление и
   так зафиксировано контекстом страницы). Плюс мини-форма в hero (ФИО +
   Телефон, без остальных полей) — новый элемент, которого нет на
   главной в текущем виде.
6. Обе формы (`signup` и `hero-form`) получают реальную отправку/защиту
   в Фазе 14, здесь — только вёрстка с корректной разметкой полей под
   будущий обработчик (`name`-атрибуты, `id` формы).

### 13.0 — Разбор макета на паттерны/переиспользуемые секции

- [x] Топ-бар (телефон/почта над шапкой) и сама шапка — часть Фазы 12
  (`header-nav.php`), переиспользуются как есть, без изменений.
- [x] Hero — новый паттерн `patterns/subject-hero.php`: слева
  `.fs-subject-hero-box` (бейдж класса, `<h1>` название направления,
  короткое и длинное описание — настоящие Gutenberg-блоки `wp:paragraph`/
  `wp:heading`, НЕ статик `wp:html`), справа — тот же `wp:html`, что и на
  главной (мини-форма `#hero-form` ФИО+Телефон+кнопка + плашка `fs-hero-stats`
  из 3 статов с разделителями). Новый CSS-класс `.fs-subject-hero-box` в
  `theme.scss` — тот же визуальный язык, что у `.fs-hero-dirs` главной
  (кремовая панель, левый акцентный бордер), но с паддингом на группе, а
  не на строчной разметке (текст внутри редактируемый).
- [x] «Сделаем вместе Шаг в будущее» + 2×2 сетка карточек — переиспользуется
  готовый `patterns/features-grid.php` (Фаза 12.5) как есть, без копии.
- [x] «Как устроены занятия» — переиспользуется готовый
  `patterns/intensive-split.php` (Фаза 12.7), `id="lessons"` уже есть.
- [x] «Приготовься сделать Шаг в будущее» — новый паттерн
  `patterns/subject-contact.php`, копия `contact-section.php` (12.8) без
  поля «Направление» (решение 5); `id="signup"` сохранён. **Обновление
  2026-09-03**: поле «Класс» тоже убрано (правка пользователя — убрать
  «Класс»/«Направление» из всех форм записи, не только с главной) —
  набор полей теперь совпадает с `contact-section.php` 1:1.
- [x] «Хочешь больше?» — новый паттерн `patterns/subject-more.php`: 2
  карточки-ссылки (Учебник / Тренажёр), `<a class="wp-block-group
  is-style-card …">` (переиспользует существующий стиль карточки, не
  дублирует его CSS) со своей внутренней раскладкой (`.fs-subject-more-card`
  в `theme.scss`). Ссылки строит новый хелпер `fs_lms_theme_subject_url()`
  (`inc/PluginRoutes.php`, по образцу `fs_lms_theme_url()` Фазы 7): если
  плагин активен — слаг из `Inc\Enums\Wp\SubjectPageType::path()`, иначе
  фолбэком тот же `/{$subjectKey}/{articles|trainer}/`-паттерн локально.
- [x] Футер — `footer-columns.php` как есть (решение 4), без изменений.
- [x] `php -l`/`npm run build`/`npx stylelint` пройдены чисто для всех
  новых файлов (см. Фазу 13.5).

### 13.1–13.4 — Страницы направлений (ЕГЭ/ОГЭ/Python/Робототехника)

- [x] **Код-часть готова и общая для всех четырёх** — паттерны 13.0
  переиспользуются 1:1, `subject-hero.php` содержит контент по умолчанию
  для ЕГЭ (13.1: бейдж «11 класс», статы 84/800 ₽/до 8 — те же цифры,
  что и на главной, `ЕГЭ информатика - мокап.dc.html` недоступен в этой
  сессии, только по ссылке на облачный Claude Design проект, см. заголовок
  файла), `subject-more.php` — `$subject_key = 'inf_ege'` по умолчанию.
  Для ОГЭ/Python/Робототехники (13.2–13.4) текст бейджа/`<h1>`/описания
  и `$subject_key` в `subject-more.php` меняются вручную при вставке
  паттерна на страницу — тексты-заготовки те же, что уже в теме
  (`courses-grid.php`: «Полный разбор формата экзамена…» 9 класс, «От
  первой строчки кода…» 10 класс, «Собираем и программируем роботов…»
  5–8 класс).
- [ ] **Сборка на живых страницах предметов плагина — НЕ выполнена.**
  Проверено на dev-стенде: `wp post list` показывает только один из
  четырёх нужных предметов (`inf_oge`, ID 88; учебник/тренажёр/курсы у
  него уже есть) — `inf_ege`/`python`/`robo` в БД этого стенда не заведены
  вовсе (заведение предмета — операция плагина, `SubjectPagesService`,
  создаёт CPT/термины/страницы — за рамками работы над темой, трогать
  не стал). Вставить паттерны в контент `/inf_oge/` уже можно было бы, но
  для этого сперва нужно поправить дефолтный контент паттернов под ОГЭ
  (сейчас в них зашит текст ЕГЭ) — отложено вместе с остальными тремя,
  чтобы не собирать 1 из 4 страниц наполовину вручную без остальных.

### 13.5 — Проверка

- [x] `php -l` на все новые/изменённые `patterns/*.php`.
- [x] `npm run build`/`npx eslint`/`npx stylelint` — чисто.
- [ ] Пиксельная сверка с `ЕГЭ информатика - мокап.dc.html` (кроме
  футера — решение 4, сверяем со футером главной v4, не с макетом ЕГЭ) —
  ждёт живой сборки страниц (13.1–13.4 заблокированы отсутствием 3 из 4
  предметов в БД, см. выше).
- [ ] Адаптив — те же брейкпоинты, что в 12.10 — та же блокировка, нечего
  проверять адаптивно без собранной страницы.
- [ ] Ссылки «Открыть учебник»/«Открыть тренажёр» — код готов
  (`fs_lms_theme_subject_url()`), но живьём резолвятся только для
  `inf_oge` (единственный существующий из 4 предметов на этом стенде);
  для трёх остальных — только после того, как эти предметы заведёт
  плагин.

## Фаза 14 — Реальная обработка форм (honeypot + скрытая капча + письмо на почту)

Сейчас все формы темы — чистая вёрстка без обработчика: `#signup` в
`contact-section.php` отправляется на `fs_lms_theme_url('apply')`
(комментарий в файле явно называет это временной заглушкой), а
`#hero-form` (Фаза 12/13) вообще никуда не отправляется. Это НЕ тот же
поток, что реальная заявка на зачисление плагина (`ApplicationCallbacks`,
OTP-подтверждение email, шифрование PII, создание ученика/родителя в
БД) — это простая лид-форма «оставьте контакты, перезвоним», без
личного кабинета и БД. Плагин трогать не нужно, вся логика — в теме.

### Локальные решения (зафиксированы в обсуждении 2026-09-02)

1. **Защита — весь стек, что и в форме заявки плагина**: honeypot-поле +
   подписанный HMAC-таймер (по образцу
   `fs-lms/inc/Services/Security/FormGuardService.php`, но СВОЙ инстанс в
   теме — тема не должна тянуть классы плагина как хард-зависимость,
   логика короткая, дублируется) + Yandex SmartCaptcha (как
   `YandexSmartCaptchaProvider` плагина) — виджет + серверная проверка
   токена через API Яндекса, со своим API-ключом в настройках темы
   (независимо от конфига плагина).
2. **Получатель писем — `info@future-step.ru`** (адрес из макетов;
   `future-step39.ru`, упомянутый в задаче изначально — опечатка,
   пользователь подтвердил правильный адрес).
3. **Отправка — `wp_mail()`**, без завязки на AJAX-инфраструктуру плагина.
4. **Rate-limit** — простой transient-счётчик по хэшу IP, свой в теме
   (не через `RateLimitService` плагина).

### 14.0 — Инфраструктура

- [x] `inc/Forms.php` — новый модуль, подключён в `functions.php` (по
  списку модулей рядом с `WooCommerce`).
- [x] Функции FormGuard в теме: `fs_lms_theme_honeypot_field()`,
  `fs_lms_theme_form_timestamp_token()`, `fs_lms_theme_form_is_human()` —
  логика 1:1 с `FormGuardService` плагина (honeypot непустой → бот;
  HMAC-подпись метки времени + мин. 3 сек на заполнение). Соль —
  `FS_LMS_THEME_FORM_SALT`, если определена в `wp-config.php`, иначе
  фолбэк на `AUTH_KEY`.
- [x] Страница настроек темы (Настройки → Формы,
  `fs-lms-theme-forms`) — Site key / Server key Yandex SmartCaptcha,
  своя `register_setting` группа `fs_lms_theme_forms`, независимая от
  настроек плагина.
- [x] Подключение `https://smartcaptcha.yandexcloud.net/captcha.js` —
  условно, только если `fs_lms_theme_captcha_configured()` (оба ключа
  заданы); иначе форма отправляется без капчи, только honeypot+таймер.
- [x] Rate-limit: `transient` `fs_theme_form_{sha1(ip)}`, 5 сабмитов /
  10 минут на IP (`fs_lms_theme_form_rate_limited()`).

### 14.1 — AJAX-обработчик

- [x] `admin-ajax.php` action `fs_theme_submit_form`
  (`wp_ajax_nopriv_...` + `wp_ajax_...`), общий для всех форм темы —
  различает форму по скрытому полю `form_id` (`signup` / `hero`).
- [x] Порядок проверок: `check_ajax_referer('fs-theme-form','nonce')` →
  honeypot+HMAC-таймер → rate-limit по IP → капча (серверная проверка
  токена, если настроена) → санитизация/обязательность полей.
- [x] Санитизация (`sanitize_text_field`/`sanitize_key`/`esc_url_raw`) +
  regex-проверка формата телефона, обязательные поля — ФИО и телефон.
- [x] `wp_mail()` на `info@future-step.ru`: тема «Заявка с сайта —
  {form_id}», тело — поля формы + IP + URL страницы; `wp_mail_from`/
  `from_name` не трогали.
- [x] JSON-ответ `wp_send_json_success`/`wp_send_json_error` → JS
  показывает статус без перезагрузки страницы.

### 14.2 — Фронтенд форм

- [x] `contact-section.php` (`#signup`): honeypot-инпут
  (`.fs-form-honeypot`, off-screen CSS, не `display:none`;
  `tabindex="-1" autocomplete="off"`), скрытое поле таймера
  (`fs_form_token`), капча-виджет (условно, PHP решает на сервере —
  проще и надёжнее, чем JS-инъекция класса), скрытое поле
  `form_id=signup`; статичного `action` на форме и не было (Фаза 12,
  решение 5, уже убрано) — добавлен `data-fs-form`, submit — через JS.
  Nonce передаёт не `wp_nonce_field` в разметке, а JS из
  `window.fsLmsTheme.formNonce` (тот же `fsLmsTheme`-объект, что и у
  quick view, Фаза 10.5) — на один hidden-инпут меньше в каждой форме.
- [x] `#hero-form` (`hero.php`/`subject-hero.php`, Фаза 12/13) — был
  `<div>` с якорем-кнопкой, переведён в настоящий `<form data-fs-form>`
  с `<button type="submit">` — тот же набор скрытых полей, `form_id=hero`.
- [x] `src/js/forms.js` (новый, подключён в `src/js/theme.js` —
  `gulpfile.js`/webpack его не требует отдельной точки входа, как
  `carousels.js`/`quick-view.js`) — `preventDefault`, `FormData(form)`,
  `fetch`, дизейбл кнопки на время запроса, success/error-состояние
  (`.fs-form-message`), сброс полей (`form.reset()`) при успехе.
- [x] Стили состояний в `theme.scss`: `.fs-form-honeypot` (off-screen),
  `.fs-form-message.is-success/.is-error` (токены `ok`/`err`, Фаза 2),
  `:disabled` на `.fs-hero-form__submit`/`.fs-apply-form__submit`.

### 14.3 — Проверка

- [x] `php -l` на все новые/изменённые файлы — чисто.
- [x] `npm run build`/`npx eslint`/`npx stylelint` — чисто (кроме
  пред-существующего `!important`-предупреждения).
- [x] Ручной тест на dev-стенде (`wp eval` — напрямую вызовом функций,
  так надёжнее сетевых flaky-таймаутов curl↔Docker на Windows, которые
  наблюдались при HTTP-тестах той же логики): `fs_lms_theme_form_is_human()`
  корректно отклоняет сабмит быстрее 3 сек и принимает после; 6 подряд
  вызовов `fs_lms_theme_form_rate_limited()` на один IP — 6-й отклонён,
  первые 5 — нет. Через HTTP (`admin-ajax.php`) отдельно подтверждено:
  неверный nonce → `-1`/403 без фатальных ошибок; валидный nonce без
  честного `fs_form_token` → корректный JSON `400` «Не удалось отправить
  форму»; пустые ФИО/телефон → JSON `400` «Заполните имя и телефон» — ни
  разу не поймали PHP fatal/warning в `debug.log`.
- [ ] **Happy path (реальное письмо на `info@future-step.ru`) НЕ
  подтверждён**: `wp_mail()` в этом Docker-стенде зависает (нет
  настроенного `sendmail`/SMTP-транспорта в контейнере
  `wordpress:latest` — `which sendmail` не находит бинарник, при этом
  `sendmail_path` в php.ini на него ссылается) — инфраструктурное
  ограничение dev-стенда, не проверялось ранее и не относится к коду
  Фазы 14. На реальном хостинге с рабочим mail-транспортом должно
  отправляться (структура письма/адрес/тема — статический код, не
  зависит от транспорта). Проверить на стенде с настроенным
  SMTP/sendmail или через плагин WP Mail SMTP — отдельным шагом.
- [ ] Капча (виджет Yandex SmartCaptcha) визуально не проверялась — ключи
  не заводили (нет тестового аккаунта Yandex Cloud в этой сессии), путь
  «капча настроена → виджет рендерится → сервер проверяет токен» не
  прогонялся живьём, только прочитан по коду `YandexSmartCaptchaProvider`
  плагина как референс для `fs_lms_theme_captcha_validate()`.

Явно вне рамок Фазы 14: полноценный OTP/личный кабинет/шифрование PII —
это зона плагина (`ApplicationCallbacks`), не трогаем; интеграция с
внешними CRM (amoCRM и т.п.) — не запрошена.

---

## Фаза 15 — Редизайн страницы «О нас» по макету

Источник дизайна — `О нас - мокап.dc.html` (тот же Claude Design проект,
что Фазы 12–13). Страница `/about/` уже существует и наполнена реальным
юридическим текстом (Фаза 10.2, `patterns/about-accordion.php`) — эта
фаза НЕ переписывает контент аккордеона (12 разделов остаются как есть,
текст уже перенесён дословно из требуемого источника), а добавляет
недостающий по макету блок реквизитов организации над аккордеоном и
подгоняет визуал под v4 (шапка/футер к тому моменту уже общие с Фазами
12–13).

### Разбор макета

- Шапка (топ-бар + меню, пункт «О нас» активен) и футер (тёмный, без
  соцсетей) — те же паттерны `header-nav.php`/`footer-columns.php`, что и
  на главной v4/страницах направлений — отдельной работы не требуют,
  страница получит их автоматически после Фаз 12–13.
- Новый блок над аккордеоном, которого сейчас нет: центрированный
  заголовок «Индивидуальный предприниматель Иванов Борис Олегович» +
  разделитель + строка «ИНН …, ОГРНИП …», ниже — 4 строки реквизитов
  (место нахождения / режим работы / телефон / email), абзац про
  лицензию (№ 11193 от 02.10.2025, рег. номер в реестре, кем выдана) и
  ссылка «Посмотреть лицензию →» на PDF.
- Сам аккордеон в макете — тот же список 12 разделов, что уже в
  `about-accordion.php` (сверено по заголовкам разделов), плюс лёгкая
  правка визуала: фон вопроса `#f8f9fa` (hover `#f1f3f5`), рамка/разделители
  `#dee2e6`, шеврон с плавным поворотом на 180° при открытии — сверить с
  текущим `.fs-legal-accordion` в `src/scss/theme.scss` и подправить, если
  разошлось.

### Локальные решения

1. **Заголовок страницы.** Макет показывает `<h1>` с юридическим
   названием ИП, а не словом «О нас». Шаблон страницы (`templates/page.html`)
   печатает `wp:post-title` из заголовка WP-страницы — самое простое
   решение: сменить `post_title` самой страницы `/about/` на
   «Индивидуальный предприниматель Иванов Борис Олегович» (ручной шаг в
   админке, как и создание самой страницы в 10.2). Пункт меню «О нас» в
   `header-nav.php` от заголовка страницы не зависит (текст пункта —
   отдельная строка в паттерне навигации), не пострадает.
2. **Ширина колонки.** Макет — 900px, глобальный `templates/page.html`
   — `contentSize:720px`. Не трогаем шаблон страницы (он общий для всех
   `page.html`-страниц) — новый блок реквизитов оборачиваем в свою группу
   с `layout:{type:"constrained", contentSize:"900px"}` только на этой
   странице, аккордеон ниже остаётся в общей колонке.
3. Ссылка «Посмотреть лицензию →» — верстается сразу (заглушка `href="#"`
   или якорь на саму страницу), файл PDF-скана лицензии добавится позже
   отдельным шагом (пользователь подтвердил 2026-09-02: «просто сделай
   ссылку, потом добавим файл») — не блокировать реализацию фазы
   ожиданием файла.

### 15.0 — Блок реквизитов

- [x] Новый паттерн `patterns/about-header.php` (отдельный от
  `about-accordion.php` — проще держать «обязательный юридический
  контент, не трогать» и «маркетинговое обрамление» раздельно):
  ИНН/ОГРНИП + разделитель, `.fs-about-requisites` (2×2 сетка адрес/режим
  работы/телефон/почта, складывается в 1 колонку на 960px), абзац о
  лицензии, ссылка «Посмотреть лицензию →» (`href="#"`, решение 3) — всё
  в группе `contentSize:900px` (решение 2). Реквизиты (адрес/ИНН/№
  лицензии) — те же данные, что уже в `footer-columns.php` и в тексте
  задачи 2026-09-02, не выдуманы заново; «режим работы» нигде в
  репозитории не был зафиксирован — проставлен ориентировочно (Пн–Пт
  10:00–20:00), сверить с реальным при живой проверке.
- [x] Паттерн вставлен в контент страницы `/about/` над
  `fs-lms-theme/about-accordion` — **не ручной шаг**: страница `/about/`
  не существовала в БД dev-стенда (проверено `wp post list`, то же
  ограничение, что было в Фазе 10.2), создана через `wp eval`/
  `wp_insert_post()` с обоими паттернами в `post_content` — безопасная
  аддитивная операция (новая страница, ничего не перезаписано), тот же
  инструмент, что и обычная публикация через редактор, просто без клика
  мышью. Живьём подтверждено (`curl`+headless-скриншот): 12 разделов
  аккордеона на месте, реквизиты рендерятся 2×2, ссылка на лицензию
  присутствует.
- [x] Заголовок страницы `/about/` сразу выставлен на юридическое
  название ИП («Индивидуальный предприниматель Иванов Борис Олегович»,
  решение 1) при создании страницы — отдельный ручной шаг не понадобился.

### 15.1 — Сверка аккордеона с макетом

- [x] Цвета вопроса/рамки/hover (`#f8f9fa`/`#f1f3f5`/`#dee2e6`) и SVG-шеврон
  с поворотом на 180° — уже сделаны в Фазе 12.11 заодно с редизайном
  `fs-lms/faq-item` (см. её отчёт: `.fs-legal-accordion` получил тот же
  Mantine-`separated` вид с точными цветами страницы `/about/`). Точная
  попиксельная сверка с `О нас - мокап.dc.html` не проводилась — файл
  недоступен локально в этой сессии (см. `patterns/subject-hero.php`,
  то же ограничение для Фазы 13) — визуально на живом `/about/`
  (скриншот) выглядит согласованно с остальной дизайн-системой. Текст 12
  разделов не трогали.

### 15.2 — Проверка

- [x] `php -l` на изменённые/новые файлы — чисто.
- [x] `npm run build`/`npx stylelint` — чисто (кроме
  пред-существующего `!important`-предупреждения). Поймана и исправлена
  реальная оплошность: CSS для `.fs-about-requisites` был добавлен в
  `theme.scss`, но `npm run build` после этого не запускался вовремя —
  первый живой скриншот показал реквизиты нестилизованными (подпись и
  значение слитно на одной строке); пересборка и повторный скриншот
  подтвердили исправление.
- [x] Живой прогон `/about/` — выполнен (см. выше): заголовок, блок
  реквизитов, аккордеон, ссылка на лицензию, шапка/футер v4 — всё на
  месте, `debug.log` пуст. Отдельно наткнулись и разобрались с
  инфраструктурной ловушкой этого дев-стенда, не багом темы: новый файл
  паттерна не подхватывался WP (`WP_Block_Patterns_Registry::is_registered()`
  → `MISSING`) из-за закэшированного `site transient`
  `wp_theme_files_patterns-*` (кэширует список паттернов темы,
  инвалидируется по `filemtime` каталога `patterns/`, который в этом
  Docker-стенде на Windows, похоже, не обновляется мгновенно через
  файловый шаринг) — `wp transient delete --all` его не берёт (это
  *site*-transient, не обычный), помогло только явное
  `wp_get_theme()->delete_pattern_cache()`. Держать в уме при следующей
  живой проверке на этом же стенде, если новый паттерн «не находится»
  сразу после создания файла.

---

## Фаза 16 — Новые страницы по макетам из Claude Design (О нас v2, Магазин, Корзина, Оформление заказа, Курсы)

Источник дизайна — один и тот же Claude Design проект
(`https://claude.ai/design/p/76381180-848a-43eb-be89-7071cd1c5f9a`), по
файлу `.dc.html` на страницу; каждый файл импортирует общий `support.js`
(его тоже читать при разборе макета — общие переменные/утилиты дизайна).
Импорт — через `claude_design` MCP (`https://api.anthropic.com/v1/design/mcp`,
авторизация `/design-login`), инструмент `DesignSync` в этой сессии.

Все 5 макетов пришлют по одному за раз (пользователь сбрасывает их по
мере готовности) — реализуем страницу за страницей, не откладывая на
конец. Хедер/футер везде переиспользуются как есть (`header-nav.php`/
`footer-columns.php`, Фаза 12) — по словам пользователя они одинаковые на
всех 5 макетах, отдельно сверять с каждым файлом не нужно только если
пользователь не укажет расхождение.

**Общее правило для всех 5 страниц**: сперва искать переиспользование —
большая часть секций (в т.ч. форма записи, карточки курсов/товаров,
FAQ-аккордеон, feature-карточки) уже есть паттернами из Фаз 5/12/13;
копировать/дублировать существующий паттерн только если макет требует
структурного отличия (как `subject-contact.php` — копия `contact-section.php`
без одного поля, Фаза 13), иначе — переиспользовать 1:1. Новые паттерны
заводить только под секции, которых в теме ещё нет.

**Ширина страницы — зафиксировано 2026-09-03 (BugFix + прямое указание
пользователя)**: все страницы этой фазы — `templates/page-wide.html`
(`_wp_page_template = page-wide`), НЕ `templates/page.html`. Разница:
`page.html` хардкодит `contentSize:720px` на `<main>` (рассчитан на узкие
текстовые страницы — юридический документ был единственным кандидатом, и
тот теперь тоже переехал на широкий), `page-wide.html` — та же структура
(`wp:post-title`+`wp:post-content`, шапка/футер шаблонами) без
`contentSize`, наследует дефолт `theme.json` (`layout.contentSize`,
1200px/75rem) — тот же приём, что `templates/front-page.html` уже
использовал для главной. Паттерны страниц НЕ должны заводить свой
`contentSize`-оверрайд поверх этого (как раньше делал `about-header.php`
с 900px, отменено) — единая ширина 1200px на всех страницах темы, включая
уже сданные `/about/` (Фаза 16.1, переключена задним числом) и `/courses/`
(Фаза 16.5). `templates/page.html` (720px) остаётся в теме файлом, но
Фаза 16 им больше не пользуется — если понадобится узкая читаемая колонка
для будущей страницы, это отдельное решение по запросу пользователя, не
дефолт.

### 16.1 — Страница «О нас» (новый макет v2)

- Источник: файл `О нас - мокап.dc.html` в проекте выше.
- **Подтверждено пользователем (2026-09-03)**: это редизайн уже
  существующей страницы `/about/` (Фаза 15,
  `patterns/about-header.php`/`about-requisites`/`about-accordion.php`) —
  сверять точечно, что изменилось в новом макете, не переписывать с нуля
  то, что совпадает со старым.
- [x] Импорт макета через `DesignSync` (+ `support.js`) — читан напрямую
  из проекта (`projectId` из ссылки пользователя), `support.js` —
  сгенерированный движок рендера макетов (`sc-for`/`sc-if`-директивы),
  контента страницы не несёт, ничего из него портировать не нужно.
- [x] Сведён список секций с уже существующими паттернами Фазы 15 —
  найдены точечные расхождения (см. заголовок `patterns/about-header.php`
  и комментарий у `.fs-legal-accordion` в `theme.scss` для деталей):
  разделитель ИНН/ОГРНИП (запятая + короткая черта вместо `·` +
  полноширинного `<hr>`), блок реквизитов (вертикальный список
  «подпись: значение» вместо сетки 2×2, реальный текст режима работы —
  раньше было незафиксированное предположение), точный текст лицензии
  (настоящий рег. номер в реестре вместо продублированного номера самой
  лицензии), выравнивание ссылки на лицензию (влево, было по центру),
  размер шрифта (`md`/16px, было `sm`/14px), стиль аккордеона
  («связанный» — общая рамка с чертой между пунктами и статичным цветом
  вопроса, было — Mantine `separated`, отдельная карточка на пункт с
  переходом в акцентный цвет при открытии). Хедер/футер/12 разделов
  аккордеона/структура блока — без изменений (переиспользованы как
  есть).
- [x] Реализовано (`patterns/about-header.php`, `.fs-about-requisites`/
  `.fs-about-divider`/`.fs-legal-accordion` в `theme.scss`); `php -l`,
  `npm run build`, `npx stylelint` — чисто, новые классы подтверждены в
  собранном `theme.min.css`.
- [x] Живой прогон `/about/` — `curl` подтверждает 200 и присутствие
  новых классов (`fs-about-divider`/`fs-about-requisites`/
  `fs-legal-accordion`) в разметке; страницу пришлось пересоздать на
  dev-стенде (см. Фазу 15.0 выше — пропала из БД между сессиями).
  Пиксельная сверка со скриншотом макета не проводилась.
- [x] **BugFix/правка пользователя (2026-09-03, после первой реализации)**:
  ширина страницы. Изначально — решение 2 Фазы 15 в силе (720px общий
  шаблон + свой оверрайд 900px в `about-header.php`, «удобнее читать
  длинный юридический текст»). Пользователь сравнил с только что
  сделанной `/courses/` (1200px, Фаза 16.5) и попросил сделать `/about/`
  такой же ширины; на уточняющий вопрос (узкая читаемая колонка на 1200
  внутри широкого `<main>` — или контент во всю ширину без ограничения)
  выбрал «полностью 1200px». `contentSize:900px` убран из
  `about-header.php`, страница переключена на `templates/page-wide.html`
  (см. общее правило ширины в начале Фазы 16 выше — теперь стандарт для
  всех страниц Фазы 16, не только для этой). Живьём подтверждено `curl`:
  `--wp--style--global--content-size: 75rem` (1200px) на `/about/`, без
  отдельного `900px`-контейнера в разметке.

### 16.2 — Страница «Магазин» (новый макет)

- Источник: файл `Магазин - мокап.dc.html` в проекте выше.
- **Важно**: страница магазина (каталог WooCommerce) уже реализована в
  Фазе 10 (`inc/WooCommerce.php`, `sidebar-shop.php`, `_woocommerce.scss`,
  quick view) на прежнем визуальном языке (WoodMart-подобный) — это
  задача на редизайн существующей вёрстки под новый макет, а не с нуля;
  живая проверка каталога/сайдбара/тулбара в 10.4–10.6 частично не
  выполнена (нет активного WooCommerce+товаров на dev-стенде) — тот же
  блокер актуален и здесь.
- [x] Импорт макета через `DesignSync` (+ `support.js`).
- [x] Сверка с текущей вёрсткой: макет оказался НЕ точечной правкой цветов
  поверх Фазы 10, а структурным упрощением — чистая сетка 4 карточки без
  сайдбара (категории+фильтр цены) и без тулбара (счётчик+сортировка).
  Уточнил у пользователя явно (2 вопроса: сайдбар/тулбар — убрать
  полностью или перекрасить оставив; quick view, которого в макете нет —
  убрать или оставить) — ответ (в чате, не через `AskUserQuestion`,
  инструмент был отклонён): «просто пересобери по макету» → убрано
  полностью, без сохранения старого функционала под флагом.
- [x] **Убрано полностью** (не оставлено закомментированным/задизейбленным):
  `sidebar-shop.php` (файл удалён), зона `shop-sidebar` в `inc/Setup.php`
  (`register_sidebar` убран), `src/js/quick-view.js` (файл удалён, импорт
  и вызов в `theme.js` убраны), AJAX-хендлер `fs_quick_view` и кнопка на
  карточке (`inc/WooCommerce.php`), `quickViewNonce` в `wp_localize_script`
  (`inc/Assets.php`), весь `.fs-shop-layout`/`.fs-widget`/`.fs-quick-view*`
  CSS (`_woocommerce.scss`, файл переписан целиком).
- [x] `inc/WooCommerce.php` — новые/изменённые хуки под макет:
  `loop_shop_columns` 3→4; `woocommerce_before_shop_loop` — вводный абзац
  (`.fs-shop-intro`, только на `is_shop()`) + сняты `woocommerce_result_count`/
  `woocommerce_catalog_ordering`; карточка перестроена — бейдж категории
  теперь ПЕРЕД заголовком (было — ссылка под заголовком), ссылка карточки
  (`woocommerce_template_loop_product_link_open/close`) закрывается сразу
  после заголовка, а не после цены (иначе кнопка «В корзину» оказалась бы
  вложена в `<a>` — невалидная вложенность интерактивных элементов); новый
  хук — короткое описание (`get_short_description()`, до 18 слов); цена и
  «В корзину» объединены в один общий футер (`.fs-shop-card__footer`,
  flex space-between) одним колбэком вместо двух хуков в разных местах
  разметки по дефолту WC.
- [x] **Обнаружено и исправлено вживую (не было видно по коду)**: у
  установленной версии WooCommerce (10.2.1) для `archive-product` по
  умолчанию используется НОВЫЙ блочный шаблон (`Product Collection` block,
  `wc-block-product-template`) — не классический `woocommerce/legacy-template`,
  на который рассчитаны все хуки выше (и на который рассчитывала ещё Фаза
  10 — на момент её написания это было верно, версия WC с тех пор
  поменяла дефолт). Без собственного `templates/archive-product.html` тема
  отдавала блочный вариант — классические хуки в новой блочной разметке
  всё равно частично срабатывали (WC даёт бэк-компат), но давали
  сломанную/задвоенную разметку (бейдж после `<img>` внутри неправильно
  закрытого `<a>`, цена+кнопка дублировались — свои через хуки И блочные
  `wp:woocommerce/product-price`/`product-button`). Решение — новый
  `templates/archive-product.html` (шапка/футер шаблонами,
  `<!-- wp:woocommerce/legacy-template {"template":"archive-product"} /-->`
  в `<main>` без `contentSize` — та же ширина 1200px, что у
  `page-wide.html`) — форсирует классический рендер, на который и
  рассчитаны хуки `inc/WooCommerce.php`. Живьём подтверждено: чистая
  `<ul class="products columns-4"><li class="product">` разметка без
  дублирования.
- [x] `php -l`, `npm run build`, `npx stylelint`, `npx eslint` — чисто;
  удалённые классы/JS подтверждены отсутствующими в собранных
  `theme.min.css`/`theme.min.js`, новые (`fs-shop-card__*`/`fs-shop-intro`)
  — присутствующими.
- [x] Живой прогон — WooCommerce на dev-стенде оказался установлен и
  активен (не был в начале сессии, кто-то поставил между задачами);
  завёл 4 тестовых товара по данным макета (Учебник по ЕГЭ/Тренажёр на
  год/Python с нуля/Набор по робототехнике — та же цена/описание/бейдж).
  Наткнулся на отдельную преграду: магазин показывал заглушку
  «coming soon» (`woocommerce_coming_soon`/`woocommerce_store_pages_only`
  опции WC, включены по умолчанию при установке плагина, не связано с
  темой) — выключил на dev-стенде для проверки; **на проде эти опции
  нужно будет выключить отдельно через Настройки WooCommerce → «Запуск
  магазина», когда каталог будет готов к показу публике** (ручной шаг,
  не код темы). `curl` подтверждает: 200, `<h1>Магазин</h1>` (из
  заголовка авто-созданной WC-страницы), вводный абзац, 4 карточки с
  бейджем/описанием/футером, ширина 1200px, добавление в корзину
  (`?wc-ajax=add_to_cart`) отвечает 200. «Магазин» не добавлен ни в одно
  меню (`header-nav.php` не менялся) — подтверждено `curl` по всем 3
  живым страницам, слова «Магазин» в навигации нет нигде.

### 16.3 — Страница «Корзина»

- Источник: файл `Корзина - мокап.dc.html` в проекте выше.
- Штатная страница WooCommerce `cart`/шорткод `[woocommerce_cart]` —
  своего паттерна в теме под неё пока нет (см. backlog «остальные
  публичные страницы» выше, снимается этой задачей). Стилизация — через
  хуки/классы WooCommerce, тот же подход, что у страницы магазина
  (Фаза 10), а не отдельный Gutenberg-шаблон с нуля.
- [x] Импорт макета через `DesignSync` (+ `support.js`).
- [x] **Обнаружено при разборе (не было видно по коду Фазы 10)**: страница
  Cart, которую создаёт WooCommerce по умолчанию, — на блоке
  `woocommerce/cart` (React-рендер, Cart & Checkout Blocks), НЕ на
  шорткоде `[woocommerce_cart]` — та же ситуация, что архив товаров в
  Фазе 16.2 (новый блочный вариант вместо классического). Стилизовать
  React-блок под кастомную сетку/степпер макета непредсказуемо (свои
  классы, свой JS) — вместо этого **переключил содержимое страницы Cart
  (ID 19632) на классический шорткод `[woocommerce_cart]`** (`wp post
  update` — ручной шаг данных, не код темы, задокументирован здесь; на
  проде — тем же способом через редактор страницы или `wp-cli`).
- [x] Новый модуль `inc/Checkout.php` (зарегистрирован в `functions.php`,
  общий для Корзины и будущего Оформления заказа — Фаза 16.4):
  - `fs_lms_theme_checkout_steps( int $active_step )` — степпер «Корзина
    → Оформление заказа → Заказ завершён» по макету, переиспользуемый
    (активный шаг параметром; на «Заказ завершён» темой ничего не
    реализовано — шаг просто никогда не активен).
  - `.fs-cart-layout` — двухколоночная сетка (форма + сводка 380px)
    открывается/закрывается хуками `woocommerce_before_cart`/
    `woocommerce_after_cart` — `<form>` и `.cart-collaterals` идут
    прямыми потомками без общей обёртки в разметке WC, колонки — через
    явный `grid-column` в CSS, без правки самих шаблонов WC.
  - «Вернуться в магазин» — новый хук `woocommerce_after_cart_table`, в
    классическом `cart/cart.php` такой ссылки нет по умолчанию (решение
    макета).
  - Кросс-селл под таблицей (`woocommerce_cross_sell_display`) — снят,
    в макете его нет (тот же принцип, что тулбар/сайдбар магазина, Фаза
    16.2 — не оставлять неоформленным под макет).
- [x] `_woocommerce.scss` — степпер + `table.cart` (грид вместо табличного
  layout: `display:block` на table/thead/tbody, `tr` — CSS grid с явным
  порядком колонок под макет — миниатюра+имя объединены визуально под
  одним заголовком «Товар», remove-иконка визуально уезжает к
  «Подытогу») + `.cart_totals` (сводка заказа, карточка `surface-2`) +
  адаптив (960px — сетка в 1 колонку; таблица — свой мобильный стек
  через `grid-template-areas`, шапка таблицы скрыта).
- [x] `php -l`, `npm run build`, `npx stylelint` — чисто.
- [x] Живой прогон — WooCommerce активен на dev-стенде (см. Фазу 16.2),
  добавил товар в корзину живым HTTP-запросом (`curl` с cookie jar,
  `?add-to-cart=19638`), `curl /cart/` подтверждает: степпер (шаг 1
  активен), товар в таблице (миниатюра/название/цена/кол-во/подытог/
  remove), «Вернуться в магазин», сводка заказа с кнопкой «Оформить
  заказ» → `/checkout/`, ширина 1200px, `Fatal error`/`Warning`/
  `Deprecated` в ответе — 0. Замечено (не баг темы, конфигурация
  WooCommerce на dev-стенде): валюта — евро вместо рубля (Настройки WC →
  Общие → не выставлены), у тестовых товаров есть строка «Доставка» (не
  отмечены как виртуальные/цифровые) — оба пункта не блокируют
  реализацию, поправить в настройках WooCommerce на реальном стенде.

### 16.4 — Страница «Оформление заказа»

- Источник: файл `Оформление заказа - мокап.dc.html` в проекте выше.
- Штатная страница WooCommerce `checkout` — та же логика, что 16.3:
  стилизация полей формы оформления/способов оплаты через хуки/классы
  WooCommerce, honeypot/капча Фазы 14 не переиспользуются (там своя
  форма плагина/темы, тут — форма WooCommerce).
- [ ] Импорт макета через `DesignSync` (+ `support.js`), разбор на секции.
- [ ] Реализовать, `php -l`/`npm run build`/`stylelint`.
- [ ] Живой прогон (нужен активный WooCommerce, дойти до оформления
  заказа с товаром в корзине).

### 16.5 — Страница «Курсы» (каталог направлений)

- Источник: файл `Курсы - мокап.dc.html` в проекте выше.
- **Подтверждено пользователем (2026-09-03)**: отдельная от магазина
  (WooCommerce-абонементы) страница-каталог направлений/курсов
  (ЕГЭ/ОГЭ/Python/Робототехника, см. Фазу 13) — карточки ведут на
  существующие страницы предметов. Сама страница-каталог — **новая,
  своя, без привязки к плагину**: не через `SubjectPageType`/CPT
  плагина и не встраивается в контент плагинных страниц (в отличие от
  Фазы 13, где паттерны вставлялись в уже существующие плагинные
  страницы предметов) — новый `templates/page-*.html` (или паттерн на
  обычной странице WP) с собственным URL/слагом, ссылки на карточках —
  обычные `<a href>` на существующие URL страниц предметов (по образцу
  `fs_lms_theme_url()`/`fs_lms_theme_subject_url()` из
  `inc/PluginRoutes.php`, Фазы 7/13 — с фолбэком на локальный
  `/{slug}/`-паттерн, если плагин неактивен), без запросов к CPT плагина
  за данными предметов.
- [x] Импорт макета через `DesignSync` (+ `support.js`) — читан напрямую
  из проекта.
- [x] Слаг/URL — подтверждён пользователем: `/courses/`, пункт меню
  «Курсы» (`header-nav.php`) переключён на `home_url('/courses/')` —
  раньше вёл на `fs_lms_theme_shop_url()` (страницу магазина, Фаза 10.3),
  теперь на этот новый каталог.
- [x] Разбор макета на паттерны (без `<h1>` — рендерится `wp:post-title`,
  тот же приём, что у `/about/`, Фаза 15/16.1):
  - `patterns/courses-catalog.php` — описание + фильтр по классу (реальная
    фильтрация JS, не декоративные чипсы макета — `src/js/course-filter.js`,
    подключён в `theme.js`) + 4 карточки направлений, статичная разметка
    (не `fs-lms/course-card`, Фаза 12.6 — блоку не хватает атрибутов под
    формат/теги/вторую кнопку/примечание к цене этого макета, расширять
    схему ради одной страницы не стали, как и `subject-more.php` в Фазе
    13). Кнопка «Программа» — новый case `'overview'` в
    `fs_lms_theme_subject_url()` (`inc/PluginRoutes.php`) — корневая
    страница предмета, без прямых запросов к CPT плагина.
  - `patterns/courses-contact.php` — форма записи, «Имя родителя»+Телефон.
    Изначально добавлял поля «Класс»+«Направление» (в макете они есть) —
    пользователь тут же попросил убрать их **везде и во всех будущих
    формах тоже** (не только на главной, где их убрали раньше отдельной
    правкой) — поля убраны отсюда и из `patterns/subject-contact.php`
    (Фаза 13, там было одиночное поле «Класс»); `.fs-form-row` (была
    ненадолго вернулась в `theme.scss` под эту пару полей) удалена снова
    как неиспользуемая.
  - Хедер/футер — `header-nav.php`/`footer-columns.php` как есть.
- [x] **BugFix (найдено пользователем после первой реализации)**: две
  проблемы —
  1. Контент страницы был зажат в 720px (`templates/page.html` хардкодит
     `contentSize:720px` — рассчитан на узкие текстовые страницы вроде
     `/about/`, для маркетингового каталога с 2-колоночной сеткой карточек
     слишком узко). Решение — новый `templates/page-wide.html` (копия
     `page.html` без `contentSize`, наследует дефолт `theme.json`, 1200px
     — тот же приём, что `templates/front-page.html`), зарегистрирован в
     `theme.json` (`customTemplates`) и назначен странице `/courses/`
     (`_wp_page_template = page-wide`). На момент этой правки `/about/` не
     трогали — оставался на `templates/page.html` (720px) со своим
     оверрайдом 900px; позже в этой же сессии пользователь попросил
     привести `/about/` к той же ширине — см. правку в конце Фазы 16.1
     выше, `page-wide.html` с тех пор общий стандарт ширины для всей
     Фазы 16 (см. правило в начале раздела).
  2. В меню подсвечивалась «Главная» на любой странице — `className:
     "current-menu-item"` был захардкожен на пункте «Главная» безусловно
     (`wp:navigation-link` со статичным `url` не получает подсветку от
     ядра автоматически, в отличие от ссылок на реальный ID поста).
     Исправлено в `patterns/header-nav.php` — активный пункт вычисляется
     через `is_front_page()`/`is_page('about')`/`is_page('courses')` и
     подставляется в `className` каждого из трёх пунктов; «Учебник»/
     «Тренажёр» пока без подсветки (страниц ещё нет, ссылки — заглушка `#`).
  Оба фикса — `php -l`/`npm run build`/`stylelint` чисто, живьём
  подтверждено `curl`: `/courses/` — 1200px и подсветка «Курсы»,
  `/about/`/`/` — подсветка «О нас»/«Главная», по-прежнему 720px и полная
  ширина соответственно.
- [x] `php -l`, `npm run build`, `npx stylelint`, `npx eslint` — чисто;
  новые классы/JS-модуль подтверждены в собранных `theme.min.css`/
  `theme.min.js`.
- [x] Живой прогон — страница `/courses/` создана на dev-стенде (та же
  операция, что `/about/` в 16.1, ID 19628), `curl` подтверждает 200,
  карточки/фильтр/форма на месте, ссылка «Программа» резолвится в
  `/inf_oge/` (единственный реально существующий из 4 предметов на этом
  стенде — тот же блокер, что в Фазе 13.5). Пиксельная сверка со
  скриншотом макета не проводилась.
- [ ] Реализовать, `php -l`/`npm run build`/`stylelint`.
- [ ] Живой прогон, пиксельная сверка с макетом.

### 16.6 — Проверка (после всех 5 страниц)

- [ ] `php -l` на все новые/изменённые файлы.
- [ ] `npm run build`/`npx stylelint` — чисто.
- [ ] Пункты меню (`header-nav.php`), ведущие на эти страницы, если были
  заглушками `#` — заменить на реальные URL (аналогично Фазе 10/12).
- [ ] Адаптив — те же брейкпоинты, что в 12.10/13.5.

---

## Явно вне рамок этого плана (backlog, не сейчас)

- Динамические блоки с `WP_Query`/REST к CPT плагина (реальные курсы/преподаватели на главной).
- Слайдер отзывов, встроенная форма заявки с реальной отправкой на главной.
- Шаблоны/паттерны для остальных публичных страниц (контакты, single блога) — паттерны из Фазы 5 переиспользуемы, но сборка этих страниц в отдельную задачу. Каталог курсов/направлений — снят в Фазу 16.5.
- Фаза 12: ACF-поля/CPT/реальный бэкенд формы записи (см. «Контент — без ACF/CPT» выше) — та же граница, что и в Фазе 11.

---
# BugFix
1. [x] Ханипот fs_company должен быть скрыт от пользователя (не должен заполняться, его заполнение - признак бота) в обеих формах скрой
   — CSS-правило `.fs-form-honeypot` (off-screen `position:absolute`,
   не `display:none`, см. `theme.scss`) и разметка (`tabindex="-1"`,
   `autocomplete="off"`, `aria-hidden="true"`) уже были в коде во всех
   4 формах (`patterns/hero.php`, `subject-hero.php`,
   `contact-section.php`, `subject-contact.php`); проблема была в том,
   что `npm run build` не запускался после изменений в `theme.scss` —
   скомпилированный `assets/css/theme.min.css` не содержал правило.
   Пересобрано.
2. [x] Кнопки splide__arrow splide__arrow--next и splide__arrow splide__arrow--prev должны быть по центру (по вертикали) контейнера splide__track splide__track--loop splide__track--ltr splide__track--draggable у правого и левого края контейнера
   — `.fs-carousel-mask .splide__arrow` (`theme.scss`) уже содержит
   `position:absolute; top:50%; transform:translateY(-50%)`; та же
   причина, что в п.1 — не собранный билд.
3. [x] Контейнер с вузами должен иметь высоту 160 пикселей splide fs-carousel-mask splide--loop splide--ltr splide--draggable is-active is-initialized
   — добавлен модификатор `.fs-carousel-mask--strip` на карусель
   вузов (`patterns/alumni-strip.php`, класс отдельный от
   `.fs-carousel-mask`, т.к. карусель выпускников `alumni-carousel.php`
   использует ту же базовую разметку с высотой по контенту, а не
   фиксированной): `height:160px` на `.splide`,
   `height:100%` на `.splide__track`/`.splide__list`,
   `align-items:center` на `.splide__list` — логотипы (`.fs-alumni-logo`,
   3.25rem) центрированы внутри 160px контейнера, а не прижаты к
   верхнему краю. `npm run build`/`stylelint`/`php -l` — чисто.