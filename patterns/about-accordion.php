<?php
/**
 * Title: Сведения об образовательной организации (юридический аккордеон)
 * Slug: fs-lms-theme/about-accordion
 * Categories: fs-lms-sections
 * Keywords: о нас, лицензия, педсостав, аккордеон, юридическая информация
 *
 * Обязательная по ст. 29 ФЗ «Об образовании в РФ» страница организации
 * (структура/руководство/педсостав/платные услуги и т.д.), а не
 * маркетинговый рассказ о школе. Контент перенесён как есть с текущего
 * сайта (`refs/Об образовательной организации - ЕГЭ по информатике.html`,
 * URL `/about/`) — придумывать/переформулировать не нужно.
 *
 * Разметка — `<details>/<summary>` `.fs-legal-accordion` (стили в
 * `src/scss/theme.scss`), а не `fs-lms/faq-item`: разделам этой страницы
 * нужен свой «юридический» вид.
 *
 * 2026-09-13: вводный абзац и ответы — тот же стиль текста `.fs-legal-text`,
 * что у `about-header.php` (см. там).
 *
 * Этап 2 (2026-09-13): разделы — записи «Вопросы» в админке с местом
 * «О нас» (`inc/Showcase/Questions.php`), ответ — в визуальном редакторе
 * (абзацы, переносы, ссылки на PDF из медиатеки). Стартовые записи
 * перенесены из прежней разметки этого файла один в один
 * (`inc/Showcase/seeds/questions.php`). Вводный абзац про ст. 29 остаётся
 * здесь; нет ни одного раздела — аккордеона нет.
 */

$fs_about_items = FS_LMS_Theme_Showcase::questions()->about_markup();
?>
<!-- wp:paragraph {"className":"fs-legal-text"} -->
<p class="fs-legal-text">В соответствии со статьёй 29 Федерального закона «Об образовании в Российской Федерации» ниже опубликована информация об организации, реализующей дополнительные общеобразовательные программы.</p>
<!-- /wp:paragraph -->
<?php if ( '' !== $fs_about_items ) : ?>

<!-- wp:html -->
<div class="fs-legal-accordion">
<?php echo $fs_about_items; // phpcs:ignore WordPress.Security.EscapeOutput.OutputNotEscaped -- разделы собраны с экранированием в FS_LMS_Theme_Questions::about_markup(). ?>

</div>
<!-- /wp:html -->
<?php endif; ?>
