<?php
/**
 * Title: Реквизиты организации — блок над юридическим аккордеоном
 * Slug: fs-lms-theme/about-header
 * Categories: fs-lms-sections
 * Keywords: о нас, реквизиты, лицензия, ИП, огрнип
 *
 * Фаза 15.0 (источник дизайна — `О нас - мокап.dc.html`, тот же Claude
 * Design проект Фазы 12). Не переписывает контент аккордеона
 * (`about-accordion.php`, Фаза 10.2) — добавляется НАД ним на странице
 * `/about/` отдельным блоком: реквизиты + абзац о лицензии + ссылка на
 * скан.
 *
 * Фаза 16.1 (BugFix tasks.md, живая сверка с макетом через Design MCP,
 * 2026-09-03) — точечные правки под фактический макет (импортирован
 * заново, был недоступен в сессии Фазы 15):
 * - Разделитель ИНН/ОГРНИП — запятая вместо «·», короткая (56px)
 *   центрированная черта вместо полноширинного `is-style-wide` `<hr>`.
 * - Блок реквизитов — вертикальный список «**Подпись:** значение» на
 *   одной строке (было: сетка 2×2 с подписью НАД значением) — реальные
 *   формулировки/значения из макета: «Режим и график работы» (с
 *   реальным временем работы — было незафиксированное предположение,
 *   см. старую версию этого комментария), «Контактный телефон», «Адрес
 *   электронной почты».
 * - Текст про лицензию — точная формулировка макета, включая настоящий
 *   регистрационный номер в сводном реестре (`№Л035-01236-39/03314396`,
 *   было ошибочно продублировано с номером самой лицензии).
 * - Ссылка «Посмотреть лицензию →» — прижата влево (`align-self:flex-start`
 *   в макете), было отцентровано.
 * - Размер шрифта реквизитов/лицензии — `md` (16px, как в макете), было
 *   `sm` (14px).
 *
 * Решение 2 Фазы 15 (ОТМЕНЕНО 2026-09-03, BugFix): было — колонка
 * 900px поверх `templates/page.html` (`contentSize:720px`). Пользователь
 * попросил сделать `/about/` той же ширины, что `/courses/` (1200px,
 * Фаза 16.5) — страница переключена на `templates/page-wide.html`
 * (без ограничения, наследует дефолт темы 1200px), своя группа здесь
 * тоже больше не задаёт `contentSize` — контент идёт на всю ширину
 * `<main>`, как на `/courses/`. Пользователь подтвердил вариант «на всю
 * 1200px», зная о компромиссе для читаемости длинного юридического
 * текста (длина строки), см. заголовок `about-accordion.php`, если там
 * появится тот же комментарий.
 * Решение 1 (не изменилось): заголовок страницы (`wp:post-title`) —
 * юридическое название ИП, вручную в админке (см. tasks.md 15.0) — этот
 * паттерн НЕ дублирует `<h1>`, начинается сразу с разделителя и
 * ИНН/ОГРНИП под заголовком страницы. Точный центрированный стиль `<h1>`
 * макета (400/34px) здесь не форсируется — `wp:post-title` общий для
 * всех страниц на этих шаблонах, менять его стиль ради одной страницы не
 * стали.
 * BugFix.5 (2026-09-12): ссылка «Посмотреть лицензию →» ведёт на запись
 * лицензии в реестре Рособрнадзора (раньше была заглушка `href="#"`).
 * Страницы, куда паттерн уже вставлен блоками, переводит
 * `FS_LMS_Theme_Content_Upgrades` (`inc/ContentUpgrades.php`).
 *
 * Реквизиты (адрес/телефон/почта) — те же, что в `footer-columns.php`
 * (единственный источник этих данных в теме, помимо самого макета);
 * ИНН/ОГРНИП — тоже оттуда (футер называет второй номер «ОГРН», хотя
 * для ИП корректно «ОГРНИП» — не отдельная ошибка этой фазы, тот же
 * номер, только точная подпись).
 *
 * 2026-09-07: страница переведена на оформление `/courses/` — текст
 * слева, без центрирования. Заголовок — `wp:post-title` шаблона
 * `templates/page-wide.html`, размер `xxl` (`has-xxl-font-size`,
 * 2.125rem — как заголовки `/articles/`/`/tasks/`/`/shop/`); размер
 * задан прямо в шаблоне, потому что обе его страницы (`/about/` и
 * `/courses/`) теперь одного размера, и точечный `.page-about` scope в
 * `theme.scss` больше не нужен — правило удалено. Оно, к слову, и не
 * работало: размер шрифта приходит пресет-классом блока, а WP печатает
 * такие классы в глобальных стилях с `!important`, так что прежний
 * `font-size: xl` из BugFix.10 не применялся никогда — заголовок всё
 * это время оставался `xxxl`, менялось только выравнивание.
 * Центрированная черта под заголовком (`.fs-about-divider`) убрана
 * совсем, вслед за `/courses/`, где разделителя между заголовком и
 * описанием никогда не было; строка ИНН/ОГРНИП тоже больше не
 * центрируется.
 *
 * 2026-09-13 (по указанию пользователя): весь текст страницы — один стиль
 * `.fs-legal-text` (`theme.scss`), общий с реквизитами и ответами
 * аккордеона. Раньше у каждого абзаца был свой пресет: 19px светлого
 * начертания, 18px, 16px приглушённого цвета — и разная высота строки.
 * Обёртка `fs-section` снята: её нижний отступ (3.5rem) давал 90px между
 * «Посмотреть лицензию» и первым абзацем `about-accordion.php` вместо
 * обычных 24px между блоками. Паттерн — несколько блоков подряд, поэтому
 * все отступы между ними одинаковые (`blockGap` страницы).
 */
?>
<!-- wp:paragraph {"className":"fs-legal-text"} -->
<p class="fs-legal-text">ИНН 390407910400, ОГРНИП 322390000000350</p>
<!-- /wp:paragraph -->

<!-- wp:html -->
<div class="fs-about-requisites">
	<div class="fs-about-requisites__item"><b>Место нахождения:</b> 236006, г. Калининград, ул. Черняховского, д. 6, каб. 316</div>
	<div class="fs-about-requisites__item"><b>Режим и график работы:</b> с понедельника по субботу с 11.00 до 21.00 (по московскому времени)</div>
	<div class="fs-about-requisites__item"><b>Контактный телефон:</b> <a href="tel:+79953264486">+7 (995) 326-44-86</a></div>
	<div class="fs-about-requisites__item"><b>Адрес электронной почты:</b> <a href="mailto:info@future-step.ru">info@future-step.ru</a></div>
</div>
<!-- /wp:html -->

<!-- wp:paragraph {"className":"fs-legal-text"} -->
<p class="fs-legal-text">Оказание образовательных услуг осуществляется на основании лицензии на осуществление образовательной деятельности № 11193 от 02.10.2025 года (рег. номер в сводном реестре лицензий на осуществление образовательной деятельности №Л035-01236-39/03314396), выданной Министерством образования Калининградской области.</p>
<!-- /wp:paragraph -->

<!-- wp:paragraph {"className":"fs-legal-text"} -->
<p class="fs-legal-text"><a href="<?php echo esc_url( FS_LMS_THEME_LICENSE_URL ); ?>" target="_blank" rel="noopener">Посмотреть лицензию →</a></p>
<!-- /wp:paragraph -->
